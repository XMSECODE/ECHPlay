# v2.8 播放器测试矩阵

本文档用于 v2.8 及后续版本发版前回归。状态必须来自真实执行结果，不能把“代码路径存在”直接写成“已通过”。

## 一、自动化命令

构建和静态 smoke：

```bash
cd android
./tools/smoke_check.sh
```

诊断收集：

```bash
cd android
./tools/collect_diagnostics.sh
```

adb 自动播放示例：

```bash
adb shell am start \
  -n com.example.abcplaydemo/.MainActivity \
  --ez echplay_auto_play true \
  --es echplay_source "rtsp://120.24.161.118:8554/live" \
  --es echplay_source_mode network \
  --es echplay_rtsp_transport tcp \
  --es echplay_render_mode auto \
  --es echplay_decode_mode auto \
  --el echplay_auto_capture_delay_ms 3000 \
  --el echplay_auto_record_duration_ms 5000
```

## 二、构建与产物

| 测试项 | 命令 | 预期结果 | 当前状态 |
| --- | --- | --- | --- |
| Debug AAR | `./tools/smoke_check.sh` | `echplayer-debug.aar` 生成 | 已纳入自动检查 |
| Release AAR | `./tools/smoke_check.sh` | `echplayer-release.aar` 生成 | 已纳入自动检查 |
| Demo APK | `./tools/smoke_check.sh` | `app-debug.apk` 生成 | 已纳入自动检查 |
| AAR ABI | `./tools/smoke_check.sh` | `arm64-v8a` so 全部存在 | 已纳入自动检查 |
| Maven Local | `./tools/publish_maven_local.sh` | 发布当前版本坐标 | 手动执行 |

## 三、协议播放

| 协议 | 测试入口 | 预期结果 | 记录方式 |
| --- | --- | --- | --- |
| 本地 MP4 | Demo 本地文件或 assets 测试文件 | 可播放、暂停、恢复、seek | 截图 + 日志 |
| HTTP MP4 | `echplay_source=http://...mp4` | 可播放，支持 Range 时可 seek | 日志 + 进度条 |
| HTTPS MP4 | `echplay_source=https://...mp4` | 可播放或输出明确 TLS/证书错误 | 日志 |
| HLS VOD | `echplay_source=https://...m3u8` | 可播放，seek 视源而定 | 日志 |
| HLS Live | `echplay_source=https://...m3u8` | 可播放，不要求 seek | 日志 |
| RTSP TCP | `echplay_rtsp_transport=tcp` | 可连接或输出明确错误 | logcat + Demo 日志 |
| RTSP UDP | `echplay_rtsp_transport=udp` | 可连接或输出明确错误 | logcat + Demo 日志 |

## 四、播放控制

| 测试项 | 操作 | 预期结果 |
| --- | --- | --- |
| pause/resume | 播放后点击暂停和继续 | 状态正常变化，画面和声音恢复 |
| stop/replay | 点击停止后重新播放 | 资源释放并可再次播放 |
| seek middle | 点播流点击跳中间 | seek 成功或给出不可 seek 提示 |
| repeated seek | 快速拖动进度条 | 不崩溃，正在 seek 时能忽略重复请求 |
| reset source | adb 重新发送 intent | 自动应用新地址并重新播放 |

## 五、截图和录制

| 测试项 | 操作 | 预期结果 |
| --- | --- | --- |
| 手动截图 | 点击截图 | PNG 来自当前解码帧，不是 SurfaceView 截屏 |
| 自动截图 | `echplay_auto_capture_delay_ms=3000` | 播放后延迟截图 |
| 手动录制 | 点击开始录制 / 停止录制 | 输出 MKV 文件 |
| 自动短录制 | `echplay_auto_record_duration_ms=5000` | 自动开始和停止录制 |
| 录制回放 | 用播放器打开录制文件 | 文件可被识别 |

## 六、性能和稳定性

| 测试项 | 时长 | 关注指标 | 记录位置 |
| --- | --- | --- | --- |
| 本地 MP4 长播 | 2 小时 | 内存、FPS、丢帧、崩溃 | `long_play_test_template.md` |
| HTTP 长播 | 2 小时 | 网速、缓冲、重连 | `long_play_test_template.md` |
| HLS Live 长播 | 2 小时 | 卡顿、缓冲、内存 | `long_play_test_template.md` |
| RTSP TCP 长播 | 2 小时 | 断流、重连、错误码 | `long_play_test_template.md` |
| RTSP UDP 长播 | 2 小时 | 丢包、卡顿、重连 | `long_play_test_template.md` |

## 七、问题收集要求

每个失败项至少记录：

1. 设备型号和 Android 版本。
2. 播放地址和协议。
3. 渲染模式、解码模式、RTSP 传输方式。
4. Demo 日志最后 50 行。
5. `collect_diagnostics.sh` 输出目录。
6. 是否能用宿主机 FFmpeg 或 VLC 播放同一个源。
