# v2.8 测试矩阵和稳定性追平目标

版本定位：v2.8 聚焦播放器测试体系。目标是把 v2.1 到 v2.7 已完成的数据源、协议、缓冲、硬解、音频、多轨、缓存统计和 SDK 发布能力，整理成可重复执行的发版前验证流程。

## 一、核心目标

1. 扩展 `smoke_check.sh`，覆盖构建产物、AAR ABI、关键 API、自动回归入口和发版文档。
2. Demo 支持 adb 启动参数，方便脚本指定播放地址、RTSP TCP/UDP、渲染模式、解码模式、自动截图和短录制。
3. 增加诊断收集脚本，快速导出 logcat、meminfo、gfxinfo、activity 和 tombstone 摘要。
4. 增加版本化测试矩阵，把本地 MP4、HTTP、HLS、RTSP、截图、录制、seek、长播和弱网测试拆成可填写项。
5. 增加 2 小时长播记录模板，避免稳定性结果只停留在口头描述。
6. 更新 README 和 release checklist，把 v2.8 测试流程纳入发版前必走步骤。

## 二、功能分解

### 目标 1：smoke check 扩展

交付内容：

1. 构建 Debug / Release AAR 和 Demo APK。
2. 检查 Release AAR 的 `arm64-v8a` native so。
3. 检查 `ECHPlayer` 关键 API：截图、录制、RTSP 传输、property。
4. 检查 Demo 自动回归启动入口。
5. 检查 ABI、Release、v2.8 测试矩阵和验证报告文档。

验收标准：

1. 无 adb 设备时也能完成构建和静态检查。
2. 有 adb 设备时可以安装并启动 Demo。
3. 核心 API 被误删时 smoke check 能失败。

### 目标 2：adb 半自动回归入口

交付内容：

1. `MainActivity` 支持 `echplay_auto_play`。
2. 支持 `echplay_source`、`echplay_source_mode`。
3. 支持 `echplay_rtsp_transport`。
4. 支持 `echplay_render_mode`、`echplay_decode_mode`。
5. 支持自动截图延迟和短录制时长。

验收标准：

1. Surface ready 后自动开始播放。
2. 收到新的 intent 时可以重新应用启动参数。
3. 自动截图和短录制走播放器 API，不走屏幕截取。

### 目标 3：诊断收集

交付内容：

1. 新增 `android/tools/collect_diagnostics.sh`。
2. 收集 adb 设备列表、logcat、pid、meminfo、gfxinfo、activity、tombstone 列表。
3. 没有 adb 或没有设备时清晰跳过。

验收标准：

1. 脚本不会因为单项 dumpsys 失败而中断整体收集。
2. 输出目录固定在 `android/build/diagnostics/`。
3. 日志文件可以用于定位播放失败、崩溃和卡顿。

### 目标 4：测试矩阵文档

交付内容：

1. 新增 `v2.8_test_matrix.md`。
2. 新增 `long_play_test_template.md`。
3. 更新 `compatibility_matrix.md` 和 `release_checklist.md`。

验收标准：

1. 每类协议都有测试入口、预期结果和记录位置。
2. 截图、录制、seek、pause/resume/stop/reset 都有测试说明。
3. 2 小时长播模板包含设备、源、错误、内存、帧率和结论。

## 三、完成标准

1. `cd android && ./tools/smoke_check.sh` 通过。
2. `cd android && ./tools/collect_diagnostics.sh` 在无设备环境下能正常跳过。
3. v2.8 文档齐全。
4. 代码提交并推送到服务器。
