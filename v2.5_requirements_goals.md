# v2.5 多轨、字幕和 TimedText 目标

## 版本定位

v2.5 对齐 ijkplayer 的多轨信息和 TimedText 接入方式，让 ECHPlayer 能展示轨道、记录轨道选择，并支持外挂 SRT 字幕在 PlayerView 中显示。

## 目标拆解

1. 多轨接口

   - 保留 `getTrackInfo()` 轨道信息列表。
   - 新增 `selectTrack(int streamIndex)`。
   - 新增 `deselectTrack(int streamIndex)`。
   - `PROP_INT64_SELECTED_VIDEO_STREAM`、`PROP_INT64_SELECTED_AUDIO_STREAM`、`PROP_INT64_SELECTED_TIMEDTEXT_STREAM` 返回当前选择状态。

2. 外挂字幕

   - 新增 `loadExternalSubtitle(File)`。
   - 新增 `loadExternalSubtitleText(String)`。
   - 支持常见 SRT 时间格式：`00:00:01,000 --> 00:00:03,000`。
   - 播放进度刷新时按当前时间分发 `ECHTimedText`。

3. TimedText 回调

   - 保留并实用化 `OnTimedTextListener`。
   - 外挂字幕命中时分发字幕文本。
   - 离开字幕时间段时分发空字幕，用于 UI 清空显示。

4. PlayerView 字幕层

   - 在视频区域底部增加字幕 `TextView` 覆盖层。
   - PlayerView 监听 `OnTimedTextListener` 并刷新字幕。
   - 停止、释放、播放完成时清空字幕。

## 达成标准

1. 新增 API 和字段均有中文注释。
2. `IECHMediaPlayer` 暴露轨道选择方法。
3. PlayerView 能显示外挂 SRT 字幕文本。
4. 编译 smoke check 通过。

## 后续边界

当前 `selectTrack/deselectTrack` 记录 Java 层选择状态，尚未深接 native 音频流/字幕流切换。内嵌字幕解码和真实多音轨切换仍需后续 native 解码队列接入。
