# ECHPlay v1.7 需求目标分解文档

版本定位：v1.7 聚焦“PlayerView 体验增强”。v1.5 已让 PlayerView 具备 loading、buffering、error、retry 基础状态，v1.6 将补齐媒体信息和性能统计。v1.7 开始把 PlayerView 从“能用的组件”提升为“普通业务页面可直接接入的基础播放器控件”。

一句话目标：

v1.7 完成后，业务方使用 `ECHPlayerView` 即可获得播放 / 暂停 / 停止 / 截图 / 录制 / 进度条 / 当前时间 / 总时长 / seek / 控制条自动隐藏等基础体验。

============================================================
一、需求拆解
============================================================

需求 1：PlayerView 增加进度条和时间显示

目标：

1. 在 PlayerView 内部增加 SeekBar。
2. 展示当前时间和总时长。
3. 播放中周期刷新进度。
4. 直播流或不可 seek 媒体禁用进度条。
5. release 时停止定时任务。

验收标准：

1. 本地 MP4 可看到当前时间和总时长。
2. HTTP MP4 可看到当前时间和总时长。
3. RTSP / HLS Live 不支持 seek 时进度条禁用或提示清楚。
4. 退出页面不会继续刷新 UI。

建议提交：

`新增PlayerView进度条 / add PlayerView progress bar`

============================================================

需求 2：PlayerView 支持拖动 seek

目标：

1. 用户拖动 SeekBar 后调用 `ECHPlayer.seekTo()`。
2. seek 中避免重复 seek。
3. 不支持 seek 时展示提示。
4. seek 完成后恢复进度刷新。

验收标准：

1. 本地 MP4 拖动可跳转。
2. HTTP MP4 支持 seek 的测试源可跳转。
3. 不支持 seek 的流不崩溃。

建议提交：

`支持PlayerView拖动跳转 / support PlayerView seek gesture`

============================================================

需求 3：控制条自动隐藏和点击显示

目标：

1. 播放中 3 秒无操作自动隐藏控制条。
2. 点击视频区域重新显示控制条。
3. loading、buffering、error 状态下控制条保持可操作。
4. 暂停状态下控制条保持显示。

验收标准：

1. 播放中控制条会自动隐藏。
2. 点击画面后控制条恢复。
3. 错误状态 retry 仍可点击。

建议提交：

`新增PlayerView控制条自动隐藏 / add PlayerView auto-hide controls`

============================================================

需求 4：PlayerView 状态和日志增强

目标：

1. PlayerView 日志输出当前播放状态。
2. PlayerView 错误状态展示更短、更适合用户阅读的文案。
3. PlayerView retry 后清理旧错误文案。
4. README 更新 PlayerView 体验说明。

验收标准：

1. Demo 里能直接验证进度、seek、隐藏控制条。
2. 错误 URL 不崩溃，retry 仍可用。
3. Debug / Release 构建通过。

建议提交：

`完善PlayerView状态体验 / polish PlayerView state UX`

============================================================
二、完成定义
============================================================

v1.7 完成时必须满足：

1. PlayerView 有进度条、当前时间、总时长。
2. PlayerView 支持可 seek 媒体拖动跳转。
3. PlayerView 不支持 seek 时有明确提示。
4. 控制条支持自动隐藏和点击显示。
5. release 后无 UI 定时刷新泄漏。
6. README 和验证报告更新。

============================================================
三、风险与边界
============================================================

1. v1.7 不做完整全屏播放器。
2. v1.7 不做复杂手势，例如亮度、音量、倍速。
3. v1.7 不改变 Native seek 策略，只在 UI 层完善调用和保护。
