# ECHPlay v1.4 验证与回归记录

本文档记录 v1.4 MediaCodec 硬解版本的开发完成情况、构建验证结果、手工验证清单和已知边界。

============================================================
一、版本目标完成情况
============================================================

已完成目标：

1. 新增 `DECODE_MODE_AUTO`、`DECODE_MODE_SOFTWARE`、`DECODE_MODE_MEDIACODEC`。
2. 新增 `setDecodeMode(int decodeMode)` 和 `getDecodeMode()`。
3. 新增 `OPTION_DECODE_MODE` 和 `OPTION_MEDIACODEC`。
4. 新增当前解码方式、解码器名称、硬解回退原因读取能力。
5. 新增 `INFO_DECODE_MODE_CHANGED`、`INFO_MEDIACODEC_OPENED`、`INFO_MEDIACODEC_FALLBACK`、`INFO_MEDIACODEC_UNSUPPORTED`。
6. 新增 native `MediaCodecVideoDecoder` 封装。
7. CMake 已链接 `mediandk`。
8. H.264 已接入 MediaCodec MIME `video/avc`。
9. H.265 已接入 MediaCodec MIME `video/hevc`。
10. H.264 AVCC / Annex B 已做基础适配。
11. H.265 HVCC / Annex B 已做基础适配。
12. MediaCodec 输出 ByteBuffer 已整理为 YUV420P 并接入现有渲染链路。
13. 硬解失败时会回退 FFmpeg 软解，并通过 info 回调说明原因。
14. Demo 支持解码模式切换和当前解码状态展示。
15. PlayerView Demo 支持解码模式切换。
16. 截图仍保存当前解码帧 PNG，不保存 SurfaceView 画面。
17. 录制仍保存 demux packet 码流，不做屏幕录制。
18. README 已补齐 v1.4 使用说明和回归清单。

============================================================
二、构建验证
============================================================

已执行并通过：

```bash
cd android
./gradlew :app:assembleDebug -x test
```

已执行并通过：

```bash
cd android
./gradlew :echplayer:assembleDebug :app:assembleDebug -x test
```

已执行并通过：

```bash
cd android
./gradlew :echplayer:assembleRelease -x test
```

说明：

1. 当前可构建 ABI 为 `arm64-v8a`。
2. Gradle 输出中仍有 deprecated API 提示，来源于既有代码路径，本轮未新增阻断性编译问题。
3. 当前环境未连接真机播放设备，因此硬解播放效果需要后续在 Android 设备上补充实测。

============================================================
三、手工验证清单
============================================================

基础播放：

1. H.264 MP4，`DECODE_MODE_AUTO`，预期优先尝试 `mediacodec`。
2. H.264 MP4，`DECODE_MODE_SOFTWARE`，预期显示 `software`。
3. H.264 MP4，`DECODE_MODE_MEDIACODEC`，预期硬解成功或显示明确回退原因。
4. H.265 MP4，支持 HEVC 的设备上预期硬解成功。
5. H.265 MP4，不支持 HEVC 的设备上预期回退软解。

网络流：

1. RTSP H.264 TCP，`DECODE_MODE_AUTO`，预期硬解成功或回退软解。
2. RTSP H.264 UDP，`DECODE_MODE_AUTO`，预期硬解成功或回退软解。
3. RTSP 直播流 seek，预期提示不支持 seek。

Demo 展示：

1. 主 Demo 能显示目标解码模式。
2. 主 Demo 能显示当前实际解码方式。
3. 主 Demo 能显示解码器名称。
4. 主 Demo 能显示硬解失败回退原因。
5. PlayerView Demo 能通过解码模式单选项切换配置。

截图和录制：

1. 软解播放中截图，预期保存当前解码帧 PNG。
2. 硬解播放中截图，预期保存当前解码帧 PNG。
3. 软解播放中录制，预期输出 mkv 文件。
4. 硬解播放中录制，预期输出 mkv 文件。
5. 录制中 stop 或退出页面，预期录制安全停止。

生命周期：

1. 硬解播放中 pause / resume 10 次不崩溃。
2. 硬解播放中 stop 后重新播放不崩溃。
3. 本地 MP4 连续 seek 5 次不死锁。
4. 连续进入和退出 PlayerView Demo 10 次不崩溃。

性能观察：

1. 对比 H.264 1080p 软解和硬解 CPU 占用趋势。
2. 对比 H.265 1080p 软解和硬解 CPU 占用趋势。
3. 如果设备支持 4K 硬解，补充 4K H.264 / H.265 播放表现。
4. 记录是否出现花屏、绿屏、比例异常或音画不同步。

============================================================
四、已知边界
============================================================

1. v1.4 首版硬解走 ByteBuffer / YUV 输出，不做 Surface 零拷贝直出。
2. 不支持 opaque / Surface-only 输出格式的硬解器会回退软解。
3. YUV 输出优先适配常见 YUV420 planar / semiplanar / flexible。
4. H.264 / H.265 CSD 已做基础适配，但复杂封装和异常码流仍可能触发回退。
5. 当前硬解帧同步策略仍比较基础，后续可继续增强音视频同步和丢帧策略。
6. 当前没有设备黑名单 / 白名单，机型差异通过失败回退兜底。
7. 当前性能统计主要通过 Demo 状态和 logcat 观察，后续可增加 Debug 面板。

============================================================
五、建议真机记录模板
============================================================

每台设备建议记录：

1. 设备型号：
2. Android 版本：
3. CPU / SoC：
4. H.264 1080p AUTO 结果：
5. H.264 1080p SOFTWARE 结果：
6. H.265 1080p AUTO 结果：
7. RTSP H.264 TCP AUTO 结果：
8. 截图结果：
9. 录制结果：
10. 回退原因：
11. 明显问题：

============================================================
六、下一版本建议
============================================================

v1.5 建议继续推进：

1. RTSP 断流自动重连。
2. HTTP / HTTPS / HLS 播放专项验证。
3. 缓冲队列长度、网速、解码帧率和丢帧统计。
4. 设备硬解黑名单 / 白名单。
5. SurfaceTexture / Surface 零拷贝硬解渲染路径评估。
