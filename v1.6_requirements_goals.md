# ECHPlay v1.6 需求目标分解文档

版本定位：v1.6 聚焦“媒体信息和性能统计”。v1.5 已完成网络播放入口、RTSP 自动重连、基础播放统计和 PlayerView 基础状态，但仍缺少 getMediaInfo / getTrackInfo、prepare 耗时、首帧耗时、渲染帧数、丢帧数等可诊断信息。

一句话目标：

v1.6 完成后，业务方和 Demo 可以直接看到当前媒体的容器、时长、码率、音视频轨道、解码方式、prepare 耗时、首帧耗时、decode fps、render fps 和丢帧估算。

============================================================
一、承接 v1.5 的遗留边界
============================================================

v1.5 已完成：

1. `ECHPlayer.PlaybackStats` 基础统计。
2. Demo 展示读取速度、队列长度、缓冲百分比和 decode fps。
3. `INFO_VIDEO_RENDERING_START` 首帧事件。
4. v1.5 报告明确 prepare 耗时和首帧耗时仍待增强。

v1.6 承接范围：

1. 补齐媒体信息 API。
2. 补齐轨道信息 API。
3. 扩展播放统计，加入 prepare / start / 首帧时间。
4. 扩展 native 统计，加入渲染帧数、丢帧数和 render fps。
5. Demo 可直接显示媒体信息和性能统计。

v1.6 不承接：

1. 不做字幕渲染。
2. 不做音轨切换。
3. 不做 SurfaceTexture 零拷贝硬解。
4. 不做 CPU / 内存系统级 profiler。

============================================================
二、需求拆解
============================================================

需求 1：新增媒体信息 API

目标：

1. 新增 `ECHPlayer.MediaInfo` 数据类。
2. 暴露容器格式、总时长、总码率、视频宽高、视频编码、音频编码、采样率、声道数。
3. Native 层从 `AVFormatContext` 和 `AVCodecParameters` 读取信息。
4. Java API 提供 `getMediaInfo()`。
5. README 增加使用示例。

验收标准：

1. prepare 成功后能拿到非空 MediaInfo。
2. 没有音频流时音频字段为空或 0，不崩溃。
3. 本地 MP4、HTTP MP4、HLS VOD 均可查询基础信息。

建议提交：

`新增媒体信息API / add media info API`

============================================================

需求 2：新增轨道信息 API

目标：

1. 新增 `ECHPlayer.TrackInfo` 数据类。
2. 支持 video、audio、subtitle、other 四类轨道类型。
3. 每条 track 包含 stream index、类型、codec、language、width、height、sampleRate、channels。
4. Java API 提供 `getTrackInfo()`。
5. Demo 日志或状态栏展示 track 数量和核心信息。

验收标准：

1. H.264 + AAC MP4 能展示 1 条视频轨和 1 条音频轨。
2. 纯视频或无音频媒体不崩溃。
3. 字幕轨暂不渲染，只展示信息。

建议提交：

`新增轨道信息API / add track info API`

============================================================

需求 3：扩展播放性能统计

目标：

1. `PlaybackStats` 增加 prepare 耗时。
2. `PlaybackStats` 增加 start 到首帧耗时。
3. `PlaybackStats` 增加解码帧数。
4. `PlaybackStats` 增加渲染帧数。
5. `PlaybackStats` 增加丢帧数估算。
6. `PlaybackStats` 增加 render fps。
7. stop / reset / release 后统计不串数据。

验收标准：

1. prepare 成功后 prepare 耗时大于等于 0。
2. 首帧渲染后首帧耗时大于等于 0。
3. 播放过程中 render fps 和 decode fps 有变化。
4. 音视频同步丢帧时丢帧数递增。
5. 统计 API 查询不影响播放线程稳定性。

建议提交：

`扩展播放性能统计 / extend playback performance stats`

============================================================

需求 4：Demo 媒体信息和调试展示

目标：

1. 主 Demo prepare 后展示 MediaInfo。
2. 主 Demo prepare 后展示 TrackInfo。
3. 主 Demo 状态栏展示 prepare 耗时、首帧耗时、render fps、丢帧数。
4. PlayerView Demo 事件日志可看到媒体信息摘要。
5. README 更新 Debug 信息说明。

验收标准：

1. 不需要看 logcat 即可看到媒体容器、轨道和性能统计。
2. 小屏设备上按钮仍可操作。
3. 日志不无限增长。

建议提交：

`展示媒体信息和性能统计 / show media info and performance stats`

============================================================
三、完成定义
============================================================

v1.6 完成时必须满足：

1. `getMediaInfo()` 可用。
2. `getTrackInfo()` 可用。
3. `PlaybackStats` 包含 prepare 耗时、首帧耗时、解码帧数、渲染帧数、丢帧数、render fps。
4. Demo 展示媒体信息、轨道信息和扩展统计。
5. Debug / Release 构建通过。
6. README 和验证报告更新真实状态。

============================================================
四、风险与边界
============================================================

1. 不同封装格式 metadata 字段不统一，v1.6 只保证核心字段。
2. render fps 是估算值，不作为精准性能 benchmark。
3. 丢帧数只统计当前同步策略主动丢弃的视频帧。
4. 字幕和音轨切换进入后续版本。
