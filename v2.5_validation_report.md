# v2.5 验证报告

## 已实现

1. `IECHMediaPlayer` 新增 `selectTrack()` 和 `deselectTrack()`。
2. `ECHPlayer` 记录当前选中视频、音频、字幕轨道。
3. 选中轨道属性可通过 `getPropertyLong()` 读取。
4. 新增外部 SRT 字幕解析和加载接口。
5. `getCurrentPosition()` 会驱动外挂字幕按时间分发。
6. PlayerView 增加字幕覆盖层并监听 `OnTimedTextListener`。
7. 停止、释放和播放完成时清空字幕显示。

## 编译检查

已通过：`cd android && ./tools/smoke_check.sh`，Debug/Release AAR 和 Demo Debug APK 构建成功。

## 手工验证建议

1. 播放带多个 stream 的媒体，调用 `getTrackInfo()` 查看轨道列表。
2. 调用 `selectTrack(audioStreamIndex)`，确认 `PROP_INT64_SELECTED_AUDIO_STREAM` 返回选中值。
3. 加载一个 SRT 字幕文本，播放时确认 PlayerView 底部显示字幕。
4. seek 到字幕时间段前后，确认字幕能显示和清空。

## 已知边界

1. 多音轨切换当前是 Java 层选择状态，native 实际音频流切换仍待后续补齐。
2. 内嵌字幕轨当前可选择但尚未解码渲染。
3. SRT 解析覆盖常见格式，不支持 ASS/SSA 样式和复杂 HTML 标签。
