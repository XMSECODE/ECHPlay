# ECHPlay v1.3 需求目标分解文档

版本定位：v1.3 聚焦“渲染链路升级”。v1.2 已经把播放器能力拆成独立 `echplayer` library module，v1.3 不再继续扩工程结构，而是把当前 `ANativeWindow + RGBA memcpy` 的基础渲染方式，升级为更接近成熟播放器的 OpenGL ES 渲染链路。

参考依据：

1. v1.2 已完成 `echplayer` library module、AAR、JNI、NativePlayer、FFmpeg so 和 Demo app 边界拆分。
2. ijkplayer 与 ECHPlay 的差距文档中，渲染链路差距明确包含 OpenGL ES、YUV 三纹理、画面比例、TextureView / SurfaceView 选择等能力。
3. 当前 ECHPlay 主要通过 FFmpeg 解码后转 RGBA，再使用 `ANativeWindow_lock` / `memcpy` / `unlockAndPost` 渲染，性能和扩展性有限。

============================================================
一、v1.3 版本主题
============================================================

v1.3 主题：播放器渲染链路升级。

核心目标：

1. 新增 OpenGL ES 渲染器。
2. 支持 YUV420P 三纹理渲染。
3. 保留现有 RGBA / NativeWindow 渲染作为兼容路径。
4. 新增视频尺寸变化回调。
5. `ECHPlayerView` 支持画面比例模式。
6. Demo 增加渲染模式和画面比例切换入口。
7. 截图仍然保存当前解码帧数据，不依赖 Surface 截屏。

一句话标准：

v1.3 完成后，播放器库内部可以用 OpenGL ES 渲染视频画面，业务方可以通过 `ECHPlayerView` 控制画面填充策略，并能收到视频宽高变化事件。

============================================================
二、v1.3 不做什么
============================================================

以下内容不进入 v1.3 必须完成范围：

1. 不做 MediaCodec 硬解。
2. 不做 OpenSL ES / Oboe 音频输出。
3. 不做 HTTP / HLS / RTMP 专项兼容。
4. 不做自动重连。
5. 不做字幕和多音轨。
6. 不做 Maven Central 或私服发布。
7. 不把截图改成 Surface 截屏。
8. 不重写整个 NativePlayer 架构。

v1.3 的重点是视频渲染性能和展示控制，不扩散到硬解、网络、音频和发布体系。

============================================================
三、当前工程现状
============================================================

当前 v1.2 工程状态：

1. `android/echplayer` 是独立 Android library module。
2. `ECHPlayer`、`ECHPlayerView` 已位于 `com.echplay.player`。
3. native 核心位于 `android/echplayer/src/main/cpp`。
4. FFmpeg include 位于 `android/echplayer/src/main/cpp/ffmpeg/include`。
5. FFmpeg so 位于 `android/echplayer/src/main/jniLibs`。
6. app module 只作为 Demo 使用 `implementation project(':echplayer')`。

当前渲染链路：

1. FFmpeg 解码视频帧。
2. 使用 `sws_scale` 将视频帧转换为 RGBA。
3. 通过 `ANativeWindow` 锁定 Surface buffer。
4. 通过 CPU memcpy 把 RGBA 数据写入窗口。
5. 解锁并提交画面。

当前主要问题：

1. 每帧 YUV 到 RGBA 转换成本较高。
2. 每帧 CPU 拷贝成本较高。
3. 不利于后续做旋转、镜像、缩放、裁剪和滤镜。
4. `ECHPlayerView` 还没有画面比例控制。
5. Java 层没有稳定的视频尺寸变化回调。

============================================================
四、v1.3 需求分解
============================================================

需求 1：新增 OpenGL ES 渲染器骨架

目标：

1. 在 native 层新增 OpenGL ES 渲染器类，例如 `GlVideoRenderer`。
2. 负责 EGL 初始化、Surface 绑定、shader 编译、纹理创建和资源释放。
3. 支持 Surface 创建、Surface 销毁和 Surface 变更。
4. 渲染器内部方法和属性保留中文注释。
5. 失败时能返回明确错误信息，不直接崩溃。

验收标准：

1. `:echplayer:assembleDebug` 编译通过。
2. `:echplayer:assembleRelease` 编译通过。
3. 本地 MP4 播放时 OpenGL ES 初始化成功。
4. 页面退出时 EGL 和纹理资源能释放，不崩溃。

============================================================

需求 2：支持 YUV420P 三纹理渲染

目标：

1. 解码后优先保留 YUV420P 数据。
2. 使用 Y、U、V 三个纹理上传到 GPU。
3. fragment shader 内完成 YUV 到 RGB 转换。
4. 尽量减少每帧 RGBA 转换和 CPU 拷贝。
5. 如果输入像素格式暂不支持三纹理，允许回退到 RGBA 渲染路径。

验收标准：

1. YUV420P 本地 MP4 可以正常显示。
2. RTSP YUV420P 流可以正常显示或正常回退。
3. 画面颜色不明显偏色。
4. 播放过程中不出现纹理越界、黑屏或花屏。
5. 截图仍然能保存当前解码帧 PNG。

============================================================

需求 3：保留 RGBA / NativeWindow 兼容渲染路径

目标：

1. 保留 v1.2 的 RGBA + `ANativeWindow` 渲染能力。
2. 新增渲染模式配置，例如 `RENDER_MODE_AUTO`、`RENDER_MODE_OPENGL`、`RENDER_MODE_NATIVE_WINDOW`。
3. 默认使用 `AUTO`，优先 OpenGL ES，失败时回退 NativeWindow。
4. Java 层提供简单设置入口，例如 `setRenderMode(int renderMode)`。
5. Demo 页面可以切换渲染模式，方便对比和回归。

验收标准：

1. OpenGL 模式能播放。
2. NativeWindow 模式能播放。
3. AUTO 模式在 OpenGL 初始化失败时不崩溃。
4. Demo 能看到当前渲染模式日志。

============================================================

需求 4：新增视频尺寸变化回调

目标：

1. Java 层新增 `OnVideoSizeChangedListener`。
2. 回调参数至少包含视频宽、高。
3. NativePlayer 在读取流信息后记录视频宽高。
4. 视频尺寸首次可用时主动回调。
5. 如果后续流中出现尺寸变化，可以再次回调。
6. Java 层新增 `getVideoWidth()` 和 `getVideoHeight()`。

验收标准：

1. 本地 MP4 prepare 后能拿到正确视频宽高。
2. RTSP prepare 后能拿到视频宽高或返回 0。
3. `ECHPlayerView` 能根据视频宽高调整显示比例。
4. Demo 日志能显示视频尺寸变化。

============================================================

需求 5：`ECHPlayerView` 支持画面比例模式

目标：

1. 新增画面比例模式常量，例如 `SCALE_TYPE_FIT_CENTER`、`SCALE_TYPE_CENTER_CROP`、`SCALE_TYPE_FILL`、`SCALE_TYPE_ORIGINAL`。
2. `ECHPlayerView` 提供 `setScaleType(int scaleType)`。
3. 根据视频宽高和 View 宽高调整 Surface 显示区域。
4. 默认使用 `FIT_CENTER`，避免画面变形。
5. Demo 页面提供画面比例切换入口。

验收标准：

1. `FIT_CENTER` 不裁剪，保持比例完整显示。
2. `CENTER_CROP` 保持比例并裁剪边缘填满容器。
3. `FILL` 填满容器，允许变形。
4. `ORIGINAL` 尽量按视频原始尺寸显示。
5. 横屏视频和竖屏视频都能正常布局。

============================================================

需求 6：Demo 增加渲染模式和比例模式验证入口

目标：

1. `MainActivity` 增加当前渲染模式显示和切换。
2. `PlayerViewDemoActivity` 增加画面比例切换。
3. Demo 日志输出渲染模式、视频尺寸、比例模式变化。
4. 保留原有 RTSP TCP / UDP、本地文件、截图、录制、seek 入口。
5. 页面布局保持简单易懂，不引入复杂 UI。

验收标准：

1. Demo 可切换 OpenGL / NativeWindow / Auto。
2. Demo 可切换 fitCenter / centerCrop / fill / original。
3. 切换时不崩溃。
4. 切换后继续支持截图和录制。

============================================================

需求 7：截图和录制兼容 OpenGL 渲染链路

目标：

1. 截图继续使用当前解码帧数据保存 PNG。
2. 不使用 `SurfaceView` 屏幕截图作为截图结果。
3. 录制继续走 FFmpeg 封装输出，不依赖渲染方式。
4. OpenGL 模式下截图、录制行为与 NativeWindow 模式保持一致。

验收标准：

1. OpenGL 模式播放中截图成功。
2. NativeWindow 模式播放中截图成功。
3. OpenGL 模式播放中录制成功。
4. NativeWindow 模式播放中录制成功。
5. 停止播放和退出页面时录制能安全停止。

============================================================

需求 8：渲染稳定性和资源释放

目标：

1. Surface 销毁时安全停止渲染。
2. 页面退出时释放 EGL、纹理、shader、Surface 引用。
3. 停止播放后可以重新播放。
4. `reset()` 后可以重新设置数据源并播放。
5. 连续切换页面不泄漏明显 native 资源。

验收标准：

1. 连续打开和退出 `PlayerViewDemoActivity` 10 次不崩溃。
2. 连续播放、停止、重新播放 5 次不崩溃。
3. 横竖屏或 Surface 重建后不崩溃。
4. logcat 不出现明显 EGL 使用已释放 Surface 的错误。

============================================================

需求 9：README 和测试清单更新

目标：

1. README 增加 v1.3 渲染能力说明。
2. README 增加 OpenGL / NativeWindow 渲染模式说明。
3. README 增加画面比例模式说明。
4. README 增加视频尺寸回调示例。
5. README 增加 v1.3 回归测试清单。

验收标准：

1. 新人可以按 README 理解如何切换渲染模式。
2. 新人可以按 README 理解如何设置画面比例。
3. README 明确截图仍来自解码帧，不来自 Surface 截图。

============================================================
五、推荐实施顺序
============================================================

步骤 1：新增视频尺寸 API 和回调

产出：

1. `OnVideoSizeChangedListener`
2. `getVideoWidth()`
3. `getVideoHeight()`
4. NativePlayer 视频宽高记录

建议提交：

`新增视频尺寸变化回调 / add video size changed callback`

原因：

画面比例模式和 OpenGL viewport 都需要稳定的视频宽高作为基础。

============================================================

步骤 2：新增画面比例模式

产出：

1. `ECHPlayerView.setScaleType(...)`
2. fitCenter / centerCrop / fill / original
3. Demo 比例模式切换入口

建议提交：

`新增PlayerView画面比例模式 / add PlayerView scale types`

============================================================

步骤 3：新增 OpenGL ES 渲染器骨架

产出：

1. EGL 初始化和释放。
2. shader 编译。
3. Surface 绑定。
4. 空画面或简单纹理渲染验证。

建议提交：

`新增OpenGL渲染器骨架 / add OpenGL renderer skeleton`

============================================================

步骤 4：接入 YUV420P 三纹理渲染

产出：

1. Y / U / V 数据上传。
2. YUV 到 RGB shader。
3. OpenGL 模式播放画面。
4. 异常时回退 RGBA 路径。

建议提交：

`支持YUV三纹理渲染 / support YUV three-texture rendering`

============================================================

步骤 5：渲染模式配置和 Demo 切换

产出：

1. renderMode 常量。
2. `setRenderMode(int renderMode)`。
3. Demo 切换 OpenGL / NativeWindow / Auto。
4. 当前渲染模式日志。

建议提交：

`新增渲染模式切换 / add render mode switching`

============================================================

步骤 6：截图录制回归和资源释放收尾

产出：

1. OpenGL 模式截图验证。
2. OpenGL 模式录制验证。
3. Surface 销毁和播放器释放补强。
4. README 更新。

建议提交：

`补齐v1.3渲染文档和回归 / add v1.3 rendering docs and regression`

============================================================
六、v1.3 验收标准总表
============================================================

必须通过：

1. `./gradlew :echplayer:assembleDebug`
2. `./gradlew :echplayer:assembleRelease`
3. `./gradlew :app:assembleDebug -x test`
4. 本地 MP4 在 OpenGL 模式可以播放。
5. 本地 MP4 在 NativeWindow 模式可以播放。
6. AUTO 模式可以播放或正确回退。
7. `ECHPlayer` 能回调视频宽高。
8. `ECHPlayerView` 能切换 fitCenter / centerCrop / fill / original。
9. OpenGL 模式下截图保存解码帧 PNG。
10. OpenGL 模式下录制文件可生成并安全停止。
11. PlayerView Demo 可打开并验证渲染模式和比例模式。

建议通过：

1. RTSP TCP 在 OpenGL 模式可播放或返回明确错误。
2. RTSP UDP 在 OpenGL 模式可播放或返回明确错误。
3. 连续播放、停止、重新播放 5 次不崩溃。
4. 连续进入和退出 PlayerView Demo 10 次不崩溃。
5. Release AAR 仍然能生成并包含 native so。
6. Debug APK 内仍然包含 `libechplayer.so` 和 FFmpeg so。

============================================================
七、v1.3 风险点
============================================================

风险 1：OpenGL 线程和解码线程并发问题

规避：

1. 渲染器内部明确线程归属。
2. Surface 销毁时先停止渲染，再释放 EGL。
3. 共享帧数据时使用简单清晰的锁或队列。

风险 2：YUV 数据生命周期错误导致花屏或崩溃

规避：

1. 上传纹理前复制或锁定必要帧数据。
2. 不持有已经被 FFmpeg 复用的裸指针。
3. 优先实现 YUV420P，再逐步扩展其他像素格式。

风险 3：颜色空间转换偏色

规避：

1. shader 中明确 BT.601 / BT.709 转换策略。
2. 先以常见 MP4 和 RTSP 样例做对比。
3. README 标注 v1.3 首版暂不覆盖所有色彩空间。

风险 4：画面比例逻辑影响 Surface 生命周期

规避：

1. `ECHPlayerView` 只负责布局，NativePlayer 只负责渲染。
2. Surface 重建后重新设置 Surface。
3. 每次布局变化时避免重复创建播放器。

风险 5：OpenGL 路径影响截图和录制

规避：

1. 截图继续从解码帧数据生成 PNG。
2. 录制继续走 FFmpeg 封装输出。
3. 渲染路径和截图录制路径保持解耦。

============================================================
八、v1.3 完成定义
============================================================

v1.3 完成时，必须满足：

1. `echplayer` library 内有可工作的 OpenGL ES 渲染路径。
2. YUV420P 三纹理渲染能播放常见本地 MP4。
3. NativeWindow 兼容路径仍可播放。
4. Java API 提供视频尺寸回调和宽高读取。
5. `ECHPlayerView` 提供画面比例模式。
6. Demo 可以验证渲染模式和画面比例模式。
7. 截图和录制没有因 OpenGL 渲染回退。
8. README 包含 v1.3 使用说明和测试清单。
9. v1.3 变更已按阶段提交代码。

============================================================
九、v1.3 完成后再进入的下一阶段
============================================================

v1.3 完成后，建议进入 v1.4：

1. MediaCodec H.264 / H.265 硬解。
2. 软硬解切换和失败回退。
3. 当前解码方式展示。
4. 高分辨率视频性能专项验证。

原因：

OpenGL 渲染链路稳定后，再接入 MediaCodec 硬解会更自然；硬解输出纹理或 YUV 数据时，可以复用 v1.3 建立的渲染层，而不是继续扩大 NativeWindow RGBA 路径。
