# ECHPlay v1.9 验证报告

验证日期：2026-06-17

版本目标：v1.9 完成 MediaPlayer-like API 对齐，让 Java SDK 接入方式更接近 ijkplayer / Android MediaPlayer。

============================================================
一、完成内容
============================================================

1. 新增 `IECHMediaPlayer` 接口。
2. 新增 `ECHTimedText` 字幕文本占位类型。
3. `ECHPlayer` 实现 `IECHMediaPlayer`。
4. 新增 `setDisplay(SurfaceHolder holder)`。
5. 新增 `setDataSource(String, Map<String, String> headers)`。
6. 新增 `setDataSource(Context, Uri)`。
7. 新增 `setDataSource(Context, Uri, Map<String, String> headers)`。
8. 新增 `setDataSource(FileDescriptor fd)` API 入口，并明确 v2.1 完整支持。
9. 新增 `getDataSource()`。
10. 新增 `setVolume(float leftVolume, float rightVolume)`。
11. 新增 `setLooping(boolean looping)` 和 `isLooping()`。
12. 新增 `OnSeekCompleteListener`，seek 成功后独立回调。
13. 新增 `OnTimedTextListener`，v1.9 先作为字幕能力占位。
14. 新增 String option native 通道，支持 headers、user_agent、protocol_whitelist。
15. headers 会转换为 FFmpeg `headers` option 并在 `avformat_open_input` 时生效。

============================================================
二、验证命令
============================================================

执行命令：

```bash
cd android && ./tools/smoke_check.sh
```

验证结果：

1. `:echplayer:configureCMakeDebug[arm64-v8a]` 通过。
2. `:echplayer:buildCMakeDebug[arm64-v8a]` 通过。
3. `:echplayer:compileDebugJavaWithJavac` 通过。
4. `:echplayer:assembleDebug` 通过。
5. `:echplayer:assembleRelease` 通过。
6. `:app:assembleDebug` 通过。
7. Debug AAR、Release AAR、Demo Debug APK 均已生成。
8. AAR 包含 `arm64-v8a` 下的 `libechplayer.so` 和 FFmpeg so。
9. 当前无 adb 设备，脚本跳过安装和启动。

构建结论：smoke check 通过。

============================================================
三、已知边界
============================================================

1. `FileDescriptor` 数据源目前只有 API 入口，完整 Native FD 播放放到 v2.1。
2. `content://` Uri 目前会明确抛出 UnsupportedOperationException，完整 Android Uri 支持放到 v2.1。
3. TimedText 目前是 API 占位，不做真实字幕解码，真实字幕和多轨放到 v2.5。
4. 循环播放依赖媒体支持 seek，不适合 RTSP 这类不可 seek 实时流。
5. `AudioTrack.setStereoVolume` 是兼容旧 API 的做法，后续 v2.4 音频系统重构时再统一升级。

============================================================
四、完成结论
============================================================

v1.9 的目标已经完成：ECHPlay 现在具备更接近 ijkplayer `IMediaPlayer` 的 Java API 形态，并保留旧 API 兼容。后续可以继续推进 v2.0 option / property / metadata 系统。
