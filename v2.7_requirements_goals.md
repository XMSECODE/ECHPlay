# v2.7 多 ABI、发布和示例工程对齐目标

版本定位：v2.7 聚焦 SDK 发布形态。目标不是假装已经补齐所有 ABI，而是把当前真实可交付的 `arm64-v8a` AAR、Maven Local 发布、发版模板、Demo 设置页和 ABI 差距说明整理成稳定流程。

## 一、核心目标

1. 建立 Maven Local 发布入口，外部空项目可以按 `com.echplay:echplayer:2.7.0` 依赖本地 AAR。
2. 明确 ABI 策略：完整 FFmpeg so 存在的 ABI 才进入 AAR，避免构建出不可运行产物。
3. Demo 增加 ijkplayer-like 设置摘要，集中展示数据源、RTSP TCP/UDP、渲染、解码和重连配置。
4. README 补充 Maven Local 接入方式、ABI 真实状态和发布检查流程。
5. 增加 GitHub Release 模板，后续每次发版都有固定说明结构。
6. 增加 ABI 发布状态文档，明确 `arm64-v8a`、`armeabi-v7a`、`x86_64` 的差距和下一步。
7. 更新发版检查清单，把 Maven Local、ABI 状态和 License 说明纳入发版前检查。

## 二、功能分解

### 目标 1：Maven Local 发布

交付内容：

1. `android/echplayer/build.gradle` 增加 `maven-publish`。
2. 发布坐标固定为 `com.echplay:echplayer:2.7.0`。
3. Release variant 同时输出 sources jar。
4. 新增 `android/tools/publish_maven_local.sh`。

验收标准：

1. 执行 `cd android && ./tools/publish_maven_local.sh` 可以生成 Maven Local 产物。
2. `publishReleasePublicationToMavenLocal` 不影响原有 `assembleRelease`。
3. POM 里说明 FFmpeg 动态库的 LGPL 许可边界。

### 目标 2：ABI 发布状态

交付内容：

1. 新增 `abi_release_status.md`。
2. 说明当前真实可打包 ABI 是 `arm64-v8a`。
3. 说明 `armeabi-v7a` 目录已预留但缺少完整 FFmpeg so。
4. 说明 `x86_64` 暂未接入，后续需要补 CMake、FFmpeg so 和模拟器测试。

验收标准：

1. Release AAR 包含 `jni/arm64-v8a/libechplayer.so` 和 FFmpeg so。
2. 未完成 ABI 不会进入 AAR。
3. 文档不把未验证 ABI 标为已支持。

### 目标 3：Demo 设置页能力

交付内容：

1. 主 Demo 页面新增 ijkplayer-like 设置摘要。
2. 保存并恢复渲染模式、解码模式、RTSP TCP/UDP。
3. 播放日志输出当前设置摘要，方便截图发版验证。

验收标准：

1. 切换设置后摘要实时更新。
2. 重新打开 Demo 后核心设置仍能恢复。
3. 播放前日志包含设置摘要。

### 目标 4：发布资料

交付内容：

1. README 增加 Maven Local 接入示例。
2. 新增 `github_release_template.md`。
3. 更新 `release_checklist.md`。
4. 新增 `v2.7_validation_report.md`。

验收标准：

1. 新接入方能从 README 看懂 module、AAR、Maven Local 三种接入方式。
2. 发版检查清单覆盖构建、发布、ABI、License、推送和标签。
3. 验证报告记录构建和发布验证结果。

## 三、暂不完成项

1. 不直接补齐 `armeabi-v7a` 和 `x86_64` FFmpeg so，因为当前仓库没有这些 ABI 的完整动态库。
2. 不接入远端 Maven 仓库，v2.7 只保证 Maven Local。
3. 不新建复杂设置 Activity，先在主 Demo 中提供设置摘要，保持代码简单易懂。

## 四、完成标准

1. `cd android && ./tools/smoke_check.sh` 通过。
2. `cd android && ./tools/publish_maven_local.sh` 通过或在受限环境中给出明确阻塞原因。
3. v2.7 相关文档齐全。
4. 代码提交并推送到服务器。
