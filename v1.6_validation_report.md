# ECHPlay v1.6 验证报告

验证日期：2026-06-13

版本定位：v1.6 完成媒体信息、轨道信息和扩展性能统计，让播放器在 Demo 和业务 API 层具备更清晰的可观测性。

============================================================
一、功能完成情况
============================================================

1. 媒体信息 API：已完成 `ECHPlayer.MediaInfo` 和 `getMediaInfo()`。
2. 轨道信息 API：已完成 `ECHPlayer.TrackInfo` 和 `getTrackInfo()`。
3. Native 信息读取：已从 `AVFormatContext`、`AVStream` 和 `AVCodecParameters` 输出容器、码率、时长、编码、分辨率、采样率、声道数和语言信息。
4. 扩展播放统计：`PlaybackStats` 已新增 render fps、解码帧数、渲染帧数、主动丢帧数、prepare 耗时和首帧耗时。
5. Demo 展示：主 Demo prepare 后展示媒体信息和轨道信息，状态栏展示 decode/render fps、D/R/Drop 帧数、prepare 和首帧耗时。
6. PlayerView 展示：`ECHPlayerView` 在 `INFO_PREPARED` 后通过事件日志输出媒体摘要和轨道数量。

============================================================
二、构建验证
============================================================

已执行命令：

```bash
cd android
./gradlew :echplayer:assembleDebug :echplayer:assembleRelease :app:assembleDebug -x test --console=plain
```

验证结果：

1. `:echplayer:assembleDebug` 通过。
2. `:echplayer:assembleRelease` 通过。
3. `:app:assembleDebug` 通过。
4. Debug AAR、Release AAR 和 Demo Debug APK 均可生成。

构建备注：

1. Gradle 输出存在 deprecated API / Gradle 9.0 兼容性提示，本轮未展开升级。
2. 当前可直接构建 ABI 仍为 `arm64-v8a`。

============================================================
三、验收对照
============================================================

需求 1：新增媒体信息 API

状态：已完成。

说明：

1. `getMediaInfo()` 返回结构化 `MediaInfo`。
2. 无音频流时音频字段会保持空字符串或 0，不依赖音频流存在。

需求 2：新增轨道信息 API

状态：已完成。

说明：

1. `getTrackInfo()` 返回 `List<TrackInfo>`。
2. 轨道类型覆盖 video、audio、subtitle 和 other。
3. 字幕轨当前只展示信息，不渲染字幕。

需求 3：扩展播放性能统计

状态：已完成。

说明：

1. prepare 耗时由 Java 层围绕 `nativePrepare()` 记录。
2. 首帧耗时由 start 发起时间到首帧解码快照可用计算。
3. Native 层记录解码帧、渲染帧、主动丢帧和 render fps。
4. setDataSource、stop、reset、release 会重置 Java 层耗时字段，避免跨播放串数据。

需求 4：Demo 媒体信息和调试展示

状态：已完成。

说明：

1. 主 Demo prepare 成功后输出 media info 和 track info。
2. 主 Demo 固定状态栏增加 decode/render fps、D/R/Drop、prepare 和首帧耗时。
3. `ECHPlayerView` 事件日志在 prepared 后输出媒体摘要。

============================================================
四、已知边界
============================================================

1. render fps 是基于累计渲染帧数和统计起点计算的平均值，不是精准 profiler。
2. 丢帧数只统计当前音视频同步策略主动丢弃的视频帧。
3. metadata 的 language 字段取决于封装文件，可能为空。
4. 本轮未引入字幕渲染、多音轨切换、系统级 CPU / 内存 profiler。
5. 本轮仅完成构建验证，真机长时间播放和弱网压力仍建议在 v1.8 smoke / matrix 阶段统一补测。
