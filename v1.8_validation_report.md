# ECHPlay v1.8 验证报告

验证日期：2026-06-13

版本定位：v1.8 建立可重复执行的 smoke 检查、兼容性矩阵和发版检查清单，降低后续版本回归成本。

============================================================
一、功能完成情况
============================================================

1. 已新增 `android/tools/smoke_check.sh`。
2. 已新增 `compatibility_matrix.md`。
3. 已新增 `release_checklist.md`。
4. README 已增加 v1.8 测试和发版文档入口。

============================================================
二、smoke 检查结果
============================================================

已执行命令：

```bash
cd android
./tools/smoke_check.sh
```

实际检查项：

1. Debug AAR 构建。
2. Release AAR 构建。
3. Demo Debug APK 构建。
4. AAR / APK 产物存在。
5. Release AAR 包含 arm64-v8a 的 `libechplayer.so` 和 FFmpeg so。
6. 如存在 adb 设备，则安装并启动 Demo；无设备时跳过。

实际结果：通过。

执行摘要：

1. `:echplayer:assembleDebug` 通过。
2. `:echplayer:assembleRelease` 通过。
3. `:app:assembleDebug` 通过。
4. `echplayer-debug.aar` 存在。
5. `echplayer-release.aar` 存在。
6. `app-debug.apk` 存在。
7. Release AAR 包含 `jni/arm64-v8a/libechplayer.so`。
8. Release AAR 包含 `libavcodec.so`、`libavformat.so`、`libavutil.so`、`libswresample.so`、`libswscale.so`。
9. 当前环境检测到 adb 设备，Demo APK 安装成功并启动 `com.example.abcplaydemo/.MainActivity`。

验证备注：

1. 首次在受限沙箱内执行时，Gradle Wrapper 因无法写入 `~/.gradle` 锁文件失败；提权执行后正常。
2. 脚本曾使用 `unzip -l | grep -q` 检查 AAR 条目，在 `pipefail` 下会被 SIGPIPE 误报失败；已改为 `awk` 完整读取列表并严格匹配条目。

============================================================
三、v1.6-v1.8 汇总
============================================================

v1.6 已完成：

1. 媒体信息 API。
2. 轨道信息 API。
3. prepare、首帧、render fps、帧数和丢帧统计。
4. Demo 媒体信息和性能统计展示。

v1.7 已完成：

1. PlayerView 进度条、当前时间和总时长。
2. PlayerView 拖动 seek。
3. PlayerView 控制条自动隐藏和点击显示。
4. PlayerView 状态和错误文案增强。

v1.8 已完成：

1. smoke 检查脚本。
2. 兼容性矩阵。
3. 发版检查清单。
4. 最终验证报告。

============================================================
四、已知边界
============================================================

1. smoke 脚本的 adb 安装启动是可选项，无设备时不失败。
2. v1.8 不覆盖所有真机和 Android 系统版本。
3. RTSP TCP / UDP Android 侧仍保留 v1.5 已知失败记录。
4. 长时间播放和弱网压力需要稳定源与设备配合执行。
