# v2.3 MediaCodec 和渲染能力对齐目标

## 版本定位

v2.3 对齐 ijkplayer 的 MediaCodec 配置入口和渲染兼容策略，让硬解能力可选择、可屏蔽、可观测，同时保持当前 H.264/H.265 硬解链路稳定。

## 目标拆解

1. MediaCodec 选择器

   - 新增 `OnMediaCodecSelectListener`。
   - Native 在成功创建 MediaCodec 后询问 Java selector。
   - Java selector 返回 false 时回退 FFmpeg 软解。

2. MediaCodec option

   - 新增 `mediacodec-auto-rotate`。
   - 新增 `mediacodec-handle-resolution-change`。
   - 新增 `mediacodec-avc`，控制 H.264 是否走硬解。
   - 新增 `mediacodec-hevc`，控制 H.265 是否走硬解。
   - 新增 `mediacodec-mpeg4` 和 `mediacodec-mpeg2` 预留开关，当前明确保持关闭。

3. Native 硬解策略

   - H.264/H.265 继续使用现有 MediaCodec 封装。
   - 如果 codec 不支持、option 关闭、Java selector 拒绝，明确回退软解。
   - MediaCodec 输出格式变化时分发 `INFO_MEDIACODEC_FORMAT_CHANGED`。

4. 渲染兼容

   - 保持 OpenGL / NativeWindow 既有渲染回退策略。
   - 输出格式变化时 Java 层刷新视频尺寸。
   - 继续使用当前解码帧截图路径，不截 SurfaceView。

## 达成标准

1. 新增 listener、option、info code 均有中文注释。
2. `isOptionSupported()` 能识别 v2.3 新增 option。
3. Native H.264/H.265 硬解受 option 和 selector 控制。
4. 编译 smoke check 通过。

## 后续边界

MPEG2/MPEG4 MediaCodec 需要补 CSD、packet 格式转换和真机矩阵后才能打开。v2.3 先把入口和策略对齐，避免不可靠硬解路径影响现有播放稳定性。
