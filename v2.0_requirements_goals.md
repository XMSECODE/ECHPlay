# ECHPlay v2.0 需求目标分解文档

版本定位：v2.0 聚焦 option / property / metadata 系统。v1.9 已经完成 MediaPlayer-like API 形态对齐，v2.0 要把已有配置项、播放统计、媒体信息和解码器信息整理成更接近 ijkplayer 的统一入口。

一句话目标：

v2.0 完成后，业务侧可以通过统一的 `setOption(...)`、`getPropertyLong(...)`、`getPropertyFloat(...)` 和 `getMediaMeta()` 获取播放器配置、运行统计和媒体元信息。

============================================================
一、需求拆解
============================================================

需求 1：补齐 option category 和 option 快照

目标：

1. 增加 `OPTION_CATEGORY_CODEC`。
2. 增加 `OPTION_CATEGORY_SWS`。
3. 保留并兼容 `OPTION_CATEGORY_FORMAT` 和 `OPTION_CATEGORY_PLAYER`。
4. Java 层保存已设置的 long option。
5. Java 层保存已设置的 String option。
6. 新增 `getLongOptionsSnapshot()`。
7. 新增 `getStringOptionsSnapshot()`。
8. 新增 `isOptionSupported(int category, String name)`。

验收标准：

1. 所有支持的 option 调用后能在快照里看到。
2. 未支持的 option 返回 false，不静默伪装成功。
3. option 快照不会暴露内部可变 Map。

建议提交：

`补齐播放器配置分类和快照 / complete player option categories and snapshots`

------------------------------------------------------------

需求 2：新增 property long / float 入口

目标：

1. 增加 ijkplayer 风格 property 常量。
2. 新增 `getPropertyLong(int property, long defaultValue)`。
3. 新增 `getPropertyFloat(int property, float defaultValue)`。
4. long property 覆盖：选中视频流、选中音频流、视频解码器类型、音频解码器类型、视频缓存时长、音频缓存时长、视频缓存 packet、音频缓存 packet、读取字节、码率、TCP 速度、最近 seek 耗时。
5. float property 覆盖：视频解码 FPS、视频输出 FPS、播放速率、丢帧率。

验收标准：

1. property 常量能和 ijkplayer 常用属性建立映射。
2. 已有统计值可以通过 property 入口读取。
3. 尚未实现的 property 返回 defaultValue 或清晰默认值。

建议提交：

`新增播放器属性读取入口 / add player property accessors`

------------------------------------------------------------

需求 3：新增 MediaMeta 元信息结构

目标：

1. 新增 `ECHPlayer.MediaMeta`。
2. `MediaMeta` 包含 format、durationMs、bitRate、videoStreamIndex、audioStreamIndex、videoCodec、videoWidth、videoHeight、audioCodec、audioSampleRate、audioChannels、trackCount、metadata map。
3. 新增 `getMediaMeta()`。
4. `getMediaInfo()` 继续保留，作为轻量摘要 API。
5. `getMediaMeta()` 基于现有 `MediaInfo` 和 `TrackInfo` 组合，不重复调用复杂 Native 逻辑。

验收标准：

1. prepare 成功后 `getMediaMeta()` 有可读结构。
2. metadata map 为只读副本。
3. Demo 能展示 MediaMeta 摘要。

建议提交：

`新增媒体元信息结构 / add media metadata model`

------------------------------------------------------------

需求 4：新增 decoder 查询入口

目标：

1. 新增 `getVideoDecoder()`。
2. 新增 `getAudioDecoder()`。
3. `getVideoDecoder()` 返回当前实际视频解码器名称。
4. `getAudioDecoder()` 当前返回媒体音频 codec 或 `ffmpeg` 前缀的可读名称。
5. 保留 `getCurrentDecodeType()` 和 `getCurrentDecoderName()`。

验收标准：

1. Demo 可展示 videoDecoder 和 audioDecoder。
2. 硬解 / 软解时 videoDecoder 能反映当前状态。

建议提交：

`新增解码器查询入口 / add decoder query APIs`

------------------------------------------------------------

需求 5：Demo 和验证报告

目标：

1. Demo 在 prepare 成功或 info 输出时展示 MediaMeta、property 和 option 摘要。
2. 新增 `v2.0_validation_report.md`。
3. 执行 smoke check。

验收标准：

1. `android/tools/smoke_check.sh` 通过。
2. Demo 编译通过。
3. 验证报告记录已支持和待支持 property。

建议提交：

`完成v2.0验证报告 / add v2.0 validation report`

============================================================
二、完成定义
============================================================

v2.0 完成时必须满足：

1. `v2.0_requirements_goals.md` 存在。
2. `ECHPlayer` 有完整 option category 常量。
3. `ECHPlayer` 能返回 option 快照。
4. `ECHPlayer` 有 `getPropertyLong(...)` 和 `getPropertyFloat(...)`。
5. `ECHPlayer` 有 `getMediaMeta()`。
6. `ECHPlayer` 有 `getVideoDecoder()` 和 `getAudioDecoder()`。
7. Demo 能展示 v2.0 新增信息。
8. `v2.0_validation_report.md` 存在。
9. smoke check 通过。

============================================================
三、风险与边界
============================================================

1. v2.0 不实现 HTTP cache 统计，缓存系统放到 v2.6。
2. v2.0 不实现真实倍速播放，播放速率 property 当前返回默认值 1.0。
3. v2.0 不实现轨道切换，selected stream 先返回当前 best stream。
4. v2.0 不重构 Native 播放内核，只把已有统计整理为统一入口。
