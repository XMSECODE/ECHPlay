# v2.9 可选后端和跨平台预研目标

版本定位：v2.9 聚焦工程边界预研。目标是参考 ijkplayer 的多后端思路，为 ECHPlay 建立一个最小可用的 `PlayerBackend` 抽象，并提供 Android 原生 MediaPlayer fallback 原型，同时输出 ExoPlayer 和 iOS 后续路线决策。

## 一、核心目标

1. 新增 `PlayerBackend` 最小接口，抽象播放控制公约数。
2. 新增 `ECHPlayerBackend`，把现有 ECHPlayer 包装为默认主后端。
3. 新增 `AndroidMediaPlayerBackend`，提供系统播放器 fallback 原型。
4. 新增 `PlayerBackendFactory` 和 `PlayerBackendType`，集中创建后端。
5. 输出后端决策文档，明确 ECHPlayer、Android MediaPlayer、ExoPlayer 的边界。
6. 输出跨平台预研文档，明确 iOS 是否进入后续版本以及进入条件。
7. 更新 smoke check，检查后端抽象文件和 v2.9 文档。

## 二、功能分解

### 目标 1：后端接口

交付内容：

1. `PlayerBackend`。
2. `PlayerBackendType`。
3. `PlayerBackendFactory`。

验收标准：

1. 接口覆盖设置 Surface、数据源、prepare、start、pause、stop、reset、release、seek、duration、position、volume、looping、state。
2. 接口保持最小，不暴露截图、录制、FFmpeg property 等主后端专属能力。
3. 方法和类型均有中文注释。

### 目标 2：ECHPlayer 主后端适配

交付内容：

1. `ECHPlayerBackend`。
2. 允许获取内部 `ECHPlayer`，用于继续使用截图、录制、统计等扩展能力。

验收标准：

1. 主后端仍使用现有 ECHPlayer。
2. 不影响现有 `ECHPlayerView` 和 Demo。
3. 发生 prepare 错误时能通过 IOException 向统一接口传递。

### 目标 3：Android MediaPlayer fallback 原型

交付内容：

1. `AndroidMediaPlayerBackend`。
2. 覆盖系统 MediaPlayer 支持的基础播放控制。

验收标准：

1. 代码可编译。
2. 可作为简单 MP4 / 系统支持 URL 的 fallback 原型。
3. 文档明确不支持 ECHPlayer 的 FFmpeg 协议、截图、录制、缓存统计和 Native invoke。

### 目标 4：文档和验证

交付内容：

1. `v2.9_backend_decision.md`。
2. `v2.9_cross_platform_research.md`。
3. `v2.9_validation_report.md`。
4. README、compatibility matrix、release checklist 更新。

验收标准：

1. ExoPlayer 是否进入后续版本有明确结论。
2. iOS 是否进入后续版本有明确结论。
3. `cd android && ./tools/smoke_check.sh` 通过。

## 三、暂不完成项

1. 不把 `ECHPlayerView` 改造成后端可切换组件，避免影响现有播放主线。
2. 不引入 ExoPlayer 依赖，先完成决策和接口边界。
3. 不实现 iOS 工程，只输出进入条件和后续版本建议。

## 四、完成标准

1. 后端抽象代码编译通过。
2. smoke check 通过。
3. v2.9 文档齐全。
4. 代码提交并推送到服务器。
