# v2.1 验证报告

## 已实现

1. 新增 `ECHMediaDataSource` 自定义数据源接口。
2. `IECHMediaPlayer` 新增自定义数据源入口。
3. `ECHPlayer` 支持 `content://` Uri，通过 `ContentResolver.openFileDescriptor()` 转为 `/proc/self/fd/...`。
4. `ECHPlayer` 支持 `FileDescriptor` 和 `ParcelFileDescriptor` 数据源。
5. `ECHPlayer` 在新数据源、`reset()`、`release()` 时关闭自身持有的 fd。
6. `ECHPlayer` 支持把 `ECHMediaDataSource` 顺序复制到缓存文件后播放。
7. Native 支持 `stimeout` long option，并在 RTSP 打开输入时下发。

## 编译检查

已通过：`cd android && ./tools/smoke_check.sh`，Debug/Release AAR 和 Demo Debug APK 构建成功。

## 手工验证建议

1. 通过系统文件选择器选一个本地 MP4，验证 `content://` 播放。
2. 使用 `ParcelFileDescriptor.open(file, MODE_READ_ONLY)` 验证 fd 播放。
3. 设置 `headers/user_agent/protocol_whitelist/stimeout/rw_timeout/buffer_size/max_delay` 后准备 HTTP/RTSP 源。
4. 验证 `reset()` 或 `release()` 后 fd 不再泄漏。

## 已知边界

1. 普通 `FileDescriptor` 依赖 Android/Java 内部 fd 编号读取，调用方仍需保持 fd 打开。
2. `ECHMediaDataSource` 当前采用临时文件桥接，适合 VOD 或已知数据，不适合无限直播流。
3. ijkplayer 的 native AVIO hook、async/cache 协议体系仍需后续版本继续追平。
