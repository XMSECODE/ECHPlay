# ECHPlay v1.4 需求目标分解文档

版本定位：v1.4 聚焦“MediaCodec 硬解第一版”。v1.2 已完成播放器库工程化，v1.3 已完成 OpenGL ES 渲染、YUV420P 三纹理、渲染模式切换和视频尺寸回调。v1.4 的目标不是完整复刻 ijkplayer，而是在当前 ECHPlay 架构上补齐成熟播放器最核心的性能路径：H.264 / H.265 硬件解码、软硬解切换、失败回退和解码状态展示。

参考依据：

1. ijkplayer Android 侧具备 MediaCodec 硬解能力，可以通过 option 控制软解 / 硬解，并在不支持硬解时回退。
2. ijkplayer 的成熟价值不只在“能解码”，还在于解码策略、错误恢复、日志诊断和 Demo 可验证。
3. 当前 ECHPlay v1.3 已有 FFmpeg 软解、OpenGL YUV 渲染、NativeWindow 兼容渲染、截图、录制和基本状态机。
4. 当前 ECHPlay 还没有 MediaCodec、当前解码方式回调、硬解失败诊断、高分辨率性能验证和 H.265 硬解路径。

============================================================
一、v1.4 版本主题
============================================================

v1.4 主题：播放器硬解能力补齐。

核心目标：

1. 新增解码模式 API：AUTO、SOFTWARE、MEDIACODEC。
2. 新增 MediaCodec 硬解开关和 option。
3. 支持 H.264 MediaCodec 硬解。
4. 支持 H.265 MediaCodec 硬解。
5. 硬解失败时自动回退 FFmpeg 软解。
6. 新增当前解码方式、硬解 codec 名称、回退原因回调。
7. Demo 增加软解 / 硬解 / 自动切换入口和状态展示。
8. 保持 v1.3 的 OpenGL / NativeWindow 渲染、截图和录制能力不退化。

一句话标准：

v1.4 完成后，业务方可以像使用 ijkplayer 一样选择“自动 / 软解 / 硬解”解码策略，常见 H.264 / H.265 视频能优先走 MediaCodec，硬解不可用时能清晰回退到 FFmpeg 软解。

============================================================
二、对比 ijkplayer 的差距和 v1.4 取舍
============================================================

ijkplayer 已具备的关键能力：

1. MediaCodec 硬解开关。
2. H.264 / H.265 等常见编码的硬解适配。
3. 硬解失败回退软解。
4. 和 FFmpeg 解封装、packet 队列、音视频同步协同。
5. 解码方式、错误日志、性能信息可用于定位问题。
6. 长期积累了大量机型、协议和格式兼容经验。

ECHPlay v1.3 当前状态：

1. 已有 FFmpeg 软解视频线程。
2. 已有 FFmpeg 音频解码和 Java AudioTrack 输出。
3. 已有 packet 队列和基本音视频同步。
4. 已有 OpenGL YUV420P 渲染，非 YUV420P 可回退 NativeWindow RGBA。
5. 已有截图缓存和录制封装。
6. 还没有硬解、硬解 option、硬解输出帧适配和硬解诊断。

v1.4 选择先追齐的能力：

1. 追齐 MediaCodec 基础硬解，而不是一次性追齐 ijkplayer 全量功能。
2. 追齐软硬解切换和回退，而不是只做一个不可控的硬解 Demo。
3. 追齐 H.264 / H.265 两个最重要编码，而不是扩散到 VP8、VP9、AV1。
4. 追齐解码状态展示和日志，而不是只在 native 内部静默切换。
5. 优先复用 v1.3 的渲染链路，避免硬解引入后破坏截图、录制和 PlayerView。

v1.4 暂不追齐的 ijkplayer 能力：

1. 不做 ExoPlayer 后端。
2. 不做全量 codec 黑名单 / 白名单策略。
3. 不做 SurfaceTexture 零拷贝硬解渲染闭环。
4. 不做多音轨、字幕、变速、音频 OpenSL ES。
5. 不做完整弱网重连和 HTTP / HLS 专项。
6. 不做跨平台 iOS。

============================================================
三、v1.4 不做什么
============================================================

以下内容不进入 v1.4 必须完成范围：

1. 不做 Surface 直出零拷贝作为必须验收项。
2. 不做 DRM、加密流和受保护内容播放。
3. 不做 HDR、10bit、Dolby Vision 等高阶画质专项。
4. 不做所有像素格式兼容，优先处理常见 YUV420 输出。
5. 不做 HLS / RTMP / 自动重连专项。
6. 不做字幕和多音轨。
7. 不做 Oboe / OpenSL ES 音频输出。
8. 不改变 v1.3 已有公开 API 的默认行为。
9. 不把截图改成 SurfaceView 屏幕截图。
10. 不把录制改成屏幕录制。

v1.4 的重点是把“硬解是否可用、如何切换、如何回退、如何验证”做清楚。

============================================================
四、当前工程现状
============================================================

当前 v1.3 工程状态：

1. `android/echplayer` 是独立 Android library module。
2. `ECHPlayer`、`ECHPlayerView` 位于 `com.echplay.player`。
3. native 核心位于 `android/echplayer/src/main/cpp`。
4. `NativePlayer::decodeLoop()` 使用 FFmpeg `avcodec_find_decoder`、`avcodec_open2`、`avcodec_send_packet`、`avcodec_receive_frame` 软解视频。
5. `NativePlayer::tryRenderFrameWithOpenGL()` 支持 YUV420P 三纹理渲染。
6. `NativePlayer::renderFrameWithNativeWindow()` 保留 RGBA / NativeWindow 兼容渲染。
7. `ECHPlayer` 已有 `setOption(...)`、`setRenderMode(...)`、`OnInfoListener`、`OnErrorListener` 和 `OnVideoSizeChangedListener`。
8. 目前真实可打包 ABI 是 `arm64-v8a`，`armeabi-v7a` 已预留但缺少完整 FFmpeg so。

当前硬解缺口：

1. Java 层没有解码模式常量。
2. option 中没有 `mediacodec` 或 `decode_mode`。
3. native 层没有 MediaCodec / AMediaCodec 封装。
4. H.264 / H.265 packet 没有针对 MediaCodec 的格式适配。
5. 没有硬解输出帧到现有渲染链路的转换。
6. 没有硬解失败回退状态和回调。
7. Demo 无法展示当前是 software 还是 mediacodec。

============================================================
五、v1.4 需求分解
============================================================

需求 1：新增解码模式 API

目标：

1. 在 `ECHPlayer` 中新增解码模式常量，例如 `DECODE_MODE_AUTO`、`DECODE_MODE_SOFTWARE`、`DECODE_MODE_MEDIACODEC`。
2. 新增 `setDecodeMode(int decodeMode)` 和 `getDecodeMode()`。
3. 新增 option 名称，例如 `OPTION_DECODE_MODE` 和 `OPTION_MEDIACODEC`。
4. 支持字符串 option：`auto`、`software`、`mediacodec`。
5. 支持 long option：`0` 自动、`1` 软解、`2` 硬解。
6. 所有新增公开方法、常量和属性保留中文注释。

验收标准：

1. `ECHPlayer` 可以在 prepare 前设置解码模式。
2. `ECHPlayerView` 可以透传解码模式。
3. 未设置时默认 `AUTO`，保持旧版本行为优先稳定。
4. 非法 decodeMode 会自动归一到 `AUTO`。
5. README 或 v1.4 文档有 API 示例。

============================================================

需求 2：新增解码状态回调和数据模型

目标：

1. 新增 `OnDecodeInfoListener` 或复用 `OnInfoListener` 增加 info code。
2. 至少回调当前解码方式：`software` / `mediacodec`。
3. 至少回调当前硬解 codec 名称，例如 `OMX.qcom.video.decoder.avc` 或 `c2.android.avc.decoder`。
4. 至少回调回退原因，例如不支持编码、配置失败、输入 packet 失败、输出格式不支持。
5. 如果新增数据模型类，例如 `DecodeInfo` 或 `DecodeStats`，必须使用单独的类文件。

建议新增 info code：

1. `INFO_DECODE_MODE_CHANGED`：当前实际解码方式变化。
2. `INFO_MEDIACODEC_OPENED`：硬解打开成功。
3. `INFO_MEDIACODEC_FALLBACK`：硬解失败并回退软解。
4. `INFO_MEDIACODEC_UNSUPPORTED`：当前流不支持硬解。

验收标准：

1. Demo 能显示目标解码模式和实际解码方式。
2. 硬解成功时能显示 codec 名称。
3. 回退软解时能显示清晰原因。
4. 外部业务可以通过回调记录硬解启用率。

============================================================

需求 3：新增 native MediaCodec 解码器封装

目标：

1. 在 native 层新增硬解封装类，例如 `MediaCodecVideoDecoder`。
2. 优先使用 Android NDK `AMediaCodec`，保持主要解码逻辑在 native 层。
3. 封装 configure、start、queueInput、dequeueOutput、flush、stop、release。
4. 支持根据 FFmpeg codec id 选择 MIME：`video/avc`、`video/hevc`。
5. 支持读取 `AVCodecParameters` 中的宽、高、extradata 和 profile 信息。
6. MediaCodec 类方法、属性保留中文注释。
7. 构建脚本补齐 `mediandk` 依赖。

验收标准：

1. `:echplayer:assembleDebug` 编译通过。
2. `:echplayer:assembleRelease` 编译通过。
3. native 初始化和释放 MediaCodec 不崩溃。
4. 不支持的 codec id 能明确返回 unsupported。
5. release、stop、seek、reset 时 MediaCodec 资源能释放。

============================================================

需求 4：H.264 硬解适配

目标：

1. 支持 H.264 本地 MP4 硬解。
2. 支持常见 H.264 RTSP Annex B packet 的基础硬解验证。
3. 处理 SPS / PPS codec specific data。
4. 处理 AVCC 与 Annex B 格式差异。
5. MediaCodec 输入以完整 access unit 为单位。
6. packet 时间戳转换为微秒后送入 MediaCodec。

验收标准：

1. H.264 MP4 在 `DECODE_MODE_MEDIACODEC` 下可播放。
2. H.264 MP4 在 `DECODE_MODE_AUTO` 下优先尝试硬解。
3. H.264 RTSP 如果 packet 格式满足要求，可硬解或明确回退软解。
4. seek 后可以 flush MediaCodec 并继续播放。
5. 播放、暂停、恢复、停止、释放不崩溃。

============================================================

需求 5：H.265 硬解适配

目标：

1. 支持 H.265 / HEVC 本地 MP4 硬解。
2. 处理 VPS / SPS / PPS codec specific data。
3. 处理 HVCC 与 Annex B 格式差异。
4. 对设备不支持 HEVC 硬解的情况明确回退。
5. Demo 展示 HEVC 是否走硬解。

验收标准：

1. 支持 HEVC 硬解的 arm64 设备上，H.265 MP4 可硬解播放。
2. 不支持 HEVC 硬解的设备上，AUTO 模式回退软解。
3. 强制 MEDIACODEC 模式下，如果设备不支持，返回明确错误或回退策略按文档执行。
4. 回退后音视频同步和渲染不明显退化。

============================================================

需求 6：硬解输出帧接入现有渲染链路

目标：

1. v1.4 优先采用 ByteBuffer / YUV 输出路径接入现有 OpenGL YUV 渲染。
2. 硬解输出为常见 YUV420 flexible 格式时，转换为渲染器可消费的 Y / U / V 数据。
3. 如果硬解只提供 opaque / Surface-only 输出，AUTO 模式允许回退软解。
4. 不把 Surface 直出作为 v1.4 必须完成项，避免破坏截图和 PlayerView 比例逻辑。
5. 硬解输出帧仍然更新当前截图缓存。
6. 渲染失败时按渲染模式回退 NativeWindow 或软解路径。

验收标准：

1. 硬解模式下 OpenGL 渲染可以显示画面。
2. 硬解模式下截图仍能保存当前解码帧 PNG。
3. 硬解模式下录制仍走 FFmpeg packet 封装，不依赖渲染。
4. 不支持 YUV 输出的硬解器不会导致黑屏，会回退或报出明确错误。
5. PlayerView fitCenter / centerCrop / fill / original 不退化。

============================================================

需求 7：软硬解自动选择和失败回退

目标：

1. `DECODE_MODE_SOFTWARE`：只走 FFmpeg 软解。
2. `DECODE_MODE_MEDIACODEC`：优先走 MediaCodec，失败时根据策略返回错误或回退软解。
3. `DECODE_MODE_AUTO`：H.264 / H.265 优先尝试 MediaCodec，失败自动回退 FFmpeg 软解。
4. 回退前释放 MediaCodec 资源，避免同时占用硬解器和软解器。
5. 回退后继续复用现有 `decodeLoop()` 软解逻辑。
6. 回退原因必须通过 info 或 error 回调传给 Java。

建议回退原因：

1. codec id 不支持。
2. MediaCodec 创建失败。
3. MediaCodec 配置失败。
4. CSD / extradata 不完整。
5. input packet 写入失败。
6. output format 不支持。
7. seek flush 失败。

验收标准：

1. AUTO 模式下常见 H.264 / H.265 优先硬解。
2. AUTO 模式下硬解失败能自动软解播放。
3. SOFTWARE 模式不会创建 MediaCodec。
4. MEDIACODEC 模式硬解失败有明确错误或明确回退说明。
5. Demo 日志可以复盘软硬解决策过程。

============================================================

需求 8：seek、pause、resume、stop 生命周期适配

目标：

1. pause 时暂停解码输出节奏，不丢失播放器状态。
2. resume 后继续从当前 packet 队列输出。
3. seek 时清空 packet 队列并 flush MediaCodec。
4. stop 时停止 demux、视频硬解、音频解码和渲染。
5. reset / release 时释放所有 MediaCodec buffer 和 native 引用。
6. 页面退出和 Surface 销毁不导致硬解线程访问无效 Surface。

验收标准：

1. 硬解播放中暂停 / 恢复 10 次不崩溃。
2. 硬解播放中连续 seek 5 次不崩溃。
3. 硬解播放中 stop 后重新设置数据源可再次播放。
4. 页面退出时没有 native 崩溃。
5. logcat 不出现 MediaCodec released 后继续访问的错误。

============================================================

需求 9：性能统计和 Demo 展示

目标：

1. Demo 显示目标解码模式。
2. Demo 显示实际解码方式。
3. Demo 显示硬解 codec 名称。
4. Demo 显示硬解失败 / 回退原因。
5. native 层记录解码帧数、渲染帧数、丢帧数。
6. 首版统计可以通过日志和 info message 输出，后续再抽成 Debug 面板。

验收标准：

1. 播放 H.264 MP4 时 Demo 可看到 `mediacodec` 或 `software`。
2. 播放 H.265 MP4 时 Demo 可看到设备是否支持硬解。
3. 强制 SOFTWARE 时 Demo 显示软件解码。
4. 强制 MEDIACODEC 失败时 Demo 显示失败原因。
5. 日志中能看到解码帧数和回退路径。

============================================================

需求 10：文档和回归测试清单

目标：

1. README 增加 v1.4 解码模式说明。
2. README 增加 MediaCodec option 示例。
3. README 增加软硬解回退说明。
4. README 增加 H.264 / H.265 验证说明。
5. v1.4 文档记录硬解边界和不支持场景。
6. 增加发版前手动回归清单。

验收标准：

1. 新人可以按 README 设置软解 / 硬解 / 自动。
2. 新人可以理解为什么某些设备会回退软解。
3. 回归清单覆盖本地 MP4、RTSP、截图、录制、seek、暂停、释放。

============================================================
六、推荐实施顺序
============================================================

步骤 1：新增解码模式 Java API

产出：

1. `DECODE_MODE_AUTO`
2. `DECODE_MODE_SOFTWARE`
3. `DECODE_MODE_MEDIACODEC`
4. `setDecodeMode(int decodeMode)`
5. `OPTION_DECODE_MODE`
6. `OPTION_MEDIACODEC`

建议提交：

`新增解码模式配置 / add decode mode configuration`

原因：

先把外部 API 固定下来，后面的 native 硬解和 Demo 都可以围绕同一套配置实现。

============================================================

步骤 2：新增解码状态回调

产出：

1. 解码方式 info code。
2. 硬解打开 info code。
3. 硬解回退 info code。
4. Demo 状态文本。

建议提交：

`新增解码状态回调 / add decode status callbacks`

============================================================

步骤 3：新增 MediaCodecVideoDecoder 骨架

产出：

1. `MediaCodecVideoDecoder.h`
2. `MediaCodecVideoDecoder.cpp`
3. CMake / Gradle mediandk 链接。
4. codec 支持探测和资源释放。

建议提交：

`新增MediaCodec硬解骨架 / add MediaCodec decoder skeleton`

============================================================

步骤 4：接入 H.264 硬解

产出：

1. H.264 CSD 解析。
2. AVCC / Annex B packet 适配。
3. MediaCodec 输入输出循环。
4. H.264 MP4 硬解播放。

建议提交：

`支持H264硬解播放 / support H264 hardware decoding`

============================================================

步骤 5：接入 H.265 硬解

产出：

1. H.265 CSD 解析。
2. HVCC / Annex B packet 适配。
3. HEVC 设备不支持时的回退。
4. H.265 MP4 硬解播放。

建议提交：

`支持H265硬解播放 / support H265 hardware decoding`

============================================================

步骤 6：接入回退和生命周期

产出：

1. AUTO 模式软硬解决策。
2. 硬解失败回退软解。
3. seek flush。
4. pause / resume / stop / release 回归。

建议提交：

`补齐硬解回退和生命周期 / add hardware decode fallback lifecycle`

============================================================

步骤 7：Demo、README 和回归收尾

产出：

1. Demo 解码模式切换。
2. Demo 当前解码方式展示。
3. README v1.4 使用说明。
4. v1.4 测试清单。

建议提交：

`补齐v1.4硬解文档和回归 / add v1.4 hardware decode docs and regression`

============================================================
七、v1.4 验收标准总表
============================================================

必须通过：

1. `./gradlew :echplayer:assembleDebug`
2. `./gradlew :echplayer:assembleRelease`
3. `./gradlew :app:assembleDebug -x test`
4. `DECODE_MODE_SOFTWARE` 下本地 H.264 MP4 可播放。
5. `DECODE_MODE_AUTO` 下本地 H.264 MP4 优先尝试硬解。
6. `DECODE_MODE_MEDIACODEC` 下本地 H.264 MP4 可硬解播放或返回明确错误。
7. 支持 HEVC 的设备上，本地 H.265 MP4 可硬解播放。
8. 不支持 HEVC 的设备上，AUTO 模式可回退软解。
9. Demo 能显示实际解码方式。
10. 硬解失败能回调清晰原因。
11. 硬解播放中截图仍可保存 PNG。
12. 硬解播放中录制仍可开始和停止。
13. 硬解播放中 pause / resume / seek / stop / release 不崩溃。
14. v1.3 OpenGL / NativeWindow 渲染模式不退化。

建议通过：

1. H.264 RTSP TCP 在 AUTO 模式可硬解或明确回退软解。
2. H.264 RTSP UDP 在 AUTO 模式可硬解或明确回退软解。
3. 1080p H.264 硬解 CPU 占用明显低于软解。
4. 4K H.264 / H.265 在支持设备上可作为性能专项验证。
5. 连续切换软解 / 自动 / 硬解模式后重新播放不崩溃。
6. 连续进入和退出 PlayerView Demo 10 次不崩溃。
7. Release AAR 仍包含 `libechplayer.so` 和 FFmpeg so。

============================================================
八、v1.4 风险点
============================================================

风险 1：MediaCodec 输入 packet 格式不一致

规避：

1. 明确区分 MP4 AVCC / HVCC 和 RTSP Annex B。
2. 为 H.264 和 H.265 单独做 packet adapter。
3. 每种格式先用固定样例验证，再扩展协议。

风险 2：不同设备硬解输出格式差异大

规避：

1. v1.4 优先支持常见 YUV420 flexible。
2. 不支持的输出格式明确回退软解。
3. 日志中输出 color format、stride、slice height。

风险 3：硬解和现有截图机制冲突

规避：

1. 首版硬解优先走 ByteBuffer / YUV 输出。
2. 输出帧进入渲染前同步更新截图缓存。
3. Surface 直出零拷贝放到后续版本评估。

风险 4：seek 后 MediaCodec 状态不干净

规避：

1. seek 时先暂停输入输出。
2. 清空 packet 队列。
3. 调用 flush 后再恢复输入。
4. flush 失败时释放并重建 MediaCodec。

风险 5：强制硬解失败后的用户预期不一致

规避：

1. 文档明确 `AUTO` 一定允许回退。
2. `MEDIACODEC` 是否允许回退通过 option 或文档固定。
3. Demo 清楚展示“目标模式”和“实际模式”。

风险 6：实现范围过大影响 v1.4 收敛

规避：

1. P0 只做 H.264 / H.265。
2. P0 只做常见 YUV 输出。
3. Surface 直出、HDR、全量 codec 兼容全部后移。

============================================================
九、v1.4 完成定义
============================================================

v1.4 完成时，必须满足：

1. `ECHPlayer` 提供解码模式 API。
2. native 层有独立 MediaCodec 硬解封装。
3. H.264 本地 MP4 可以硬解播放。
4. H.265 本地 MP4 在支持设备上可以硬解播放。
5. AUTO 模式可以硬解优先、失败回退软解。
6. SOFTWARE 模式保持纯 FFmpeg 软解。
7. Demo 能展示目标解码模式、实际解码方式、硬解 codec 名称和回退原因。
8. 截图、录制、seek、pause、resume、stop、release 不因硬解退化。
9. README 包含 v1.4 硬解使用说明。
10. 发版前完成 v1.4 回归测试清单。

============================================================
十、v1.4 完成后再进入的下一阶段
============================================================

v1.4 完成后，建议进入 v1.5：

1. HTTP / HTTPS 播放系统验证。
2. HLS / m3u8 基础播放验证。
3. RTSP 断流自动重连。
4. 网速、缓冲百分比、队列长度统计。
5. PlayerView loading / buffering / error 状态增强。

原因：

v1.4 补齐硬解后，播放器的本地高分辨率播放性能会更接近 ijkplayer。下一阶段再补网络协议、弱网和重连，整体路线会从“能解码、能渲染”走向“能稳定播放网络流”。

============================================================
十一、v1.4 开发完成记录
============================================================

已完成提交：

1. `新增解码模式配置和状态回调 / add decode mode configuration and status callbacks`
2. `新增MediaCodec解码器骨架 / add MediaCodec decoder skeleton`
3. `支持H264和H265硬解基础路径 / support H264 and H265 hardware decode path`
4. `新增软硬解回退诊断和Demo展示 / add decode fallback diagnostics and demo display`

已完成代码能力：

1. Java API 支持自动、软解、硬解三种解码模式。
2. native 层已新增 `MediaCodecVideoDecoder`。
3. H.264 / H.265 已接入 MediaCodec 基础硬解路径。
4. 硬解输出 ByteBuffer 已转换为 YUV420P 并复用现有渲染链路。
5. 硬解失败时已具备软解回退和回退原因回调。
6. Demo 已支持解码模式切换、当前解码方式展示和回退原因展示。
7. 截图仍保存当前解码帧 PNG。
8. 录制仍保存 demux packet 码流，不做屏幕录制。
9. README 已补齐 v1.4 使用说明和回归清单。
10. 已新增 `v1.4_validation_report.md` 作为验证与回归记录。

已完成构建验证：

1. `./gradlew :app:assembleDebug -x test`
2. `./gradlew :echplayer:assembleDebug :app:assembleDebug -x test`
3. `./gradlew :echplayer:assembleRelease -x test`
4. `./gradlew :echplayer:assembleDebug :app:assembleDebug -x test --console=plain`
5. `./gradlew :app:installDebug --console=plain`

已完成模拟器实测：

1. Pixel_2_android_13 模拟器播放 H.264 MP4，AUTO 模式进入 `mediacodec`，解码器为 `video/avc`。
2. Pixel_2_android_13 模拟器播放 H.265 MP4，AUTO 模式进入 `mediacodec`，解码器为 `video/hevc`。
3. H.264 硬解路径下截图输出 PNG，保存的是当前解码帧。
4. H.264 硬解路径下录制输出 MKV，`ffprobe` 可识别 H.264 视频流和 AAC 音频流。
5. H.264 硬解路径下 pause / resume / stop 单轮验证通过，logcat 未出现崩溃。
6. Demo 本地文件模式播放按钮、截图按钮、录制按钮已在 Pixel 2 模拟器屏幕内可操作。
7. Demo 在 `prepare()` 失败时已安全返回，不再继续调用 `start()` 导致崩溃。

仍建议真机或专项补充验证：

1. H.264 / H.265 在真实 Android 设备上的硬解播放表现。
2. 不支持 HEVC 的真实设备上的 H.265 回退软解表现。
3. RTSP H.264 在 TCP / UDP 下的硬解或回退表现。
4. 硬解路径下截图、录制、pause / resume、seek、stop、release 的长时间稳定性。
5. 1080p / 4K 软硬解 CPU 占用对比。
