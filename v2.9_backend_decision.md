# v2.9 后端决策文档

本文档用于说明 ECHPlay 后续是否引入多后端，以及每个后端的职责边界。

## 一、当前结论

1. `ECHPlayerBackend` 是默认主后端。
2. `AndroidMediaPlayerBackend` 是 fallback 原型，只覆盖系统播放器支持的简单播放能力。
3. ExoPlayer 后端暂不在 v2.9 引入依赖，建议放入后续版本继续评估。
4. `PlayerBackend` 只抽象最小播放控制公约数，不承诺每个后端都支持截图、录制、FFmpeg option、property、metadata、缓存统计和 Native invoke。

## 二、后端对比

| 后端 | 当前状态 | 适用场景 | 不适用场景 |
| --- | --- | --- | --- |
| ECHPlayer | 默认主后端 | FFmpeg 协议、RTSP、截图、录制、缓存统计、MediaCodec、OpenGL | 暂无 |
| Android MediaPlayer | fallback 原型 | 系统可直接播放的本地文件、简单 HTTP/HTTPS | RTSP、复杂 HLS、截图、录制、FFmpeg 统计 |
| ExoPlayer | 待评估 | DASH/HLS、Android 媒体生态、DRM、播放器 UI 生态 | FFmpeg 自定义协议和 native 录制 |
| iOS 后端 | 待预研 | 后续跨平台 SDK | v2.9 不实现 |

## 三、为什么先做 PlayerBackend

ijkplayer Android 工程存在 `ijkplayer-java`、`ijkplayer-exo` 等不同实现方向。ECHPlay 目前主线已经有 FFmpeg/MediaCodec/OpenGL 组合，如果直接把 ExoPlayer 或系统 MediaPlayer 混进现有 `ECHPlayer`，代码会变复杂，也会让主播放器能力边界变模糊。

因此 v2.9 先做薄接口：

1. 保留 ECHPlayer 主线稳定。
2. 为 fallback 和未来 ExoPlayer 提供统一接入点。
3. 让后续 Demo 或业务方可以显式选择后端。
4. 避免一次性引入重依赖。

## 四、PlayerBackend 能力边界

`PlayerBackend` 当前只覆盖：

1. Surface。
2. DataSource。
3. prepare / start / pause / stop / reset / release。
4. seek / duration / current position。
5. playing / looping / volume。
6. backend name / state text / released。

不放入接口的能力：

1. 截图 PNG。
2. 录制文件。
3. FFmpeg option / property。
4. metadata / track info。
5. RTSP TCP/UDP。
6. cache stats。
7. Native invoke。

这些能力仍然属于 `ECHPlayer` 主后端扩展能力。需要时可以通过 `ECHPlayerBackend.getPlayer()` 访问。

## 五、ExoPlayer 后续建议

建议后续新增独立版本，例如 v3.0 或 v3.1，再评估 ExoPlayer：

1. 引入 AndroidX Media3 依赖。
2. 实现 `ExoPlayerBackend`。
3. 只承诺 HLS/DASH/HTTP 等 ExoPlayer 擅长场景。
4. 不要求 ExoPlayer 支持 ECHPlayer 的截图、录制和 FFmpeg property。
5. Demo 增加后端选择入口。

## 六、Android MediaPlayer fallback 使用建议

Android MediaPlayer fallback 适合：

1. 作为系统播放器兼容性对照。
2. 播放系统原生支持的本地 MP4。
3. 简单 HTTP/HTTPS URL 验证。

不建议：

1. 用它替代 ECHPlayer 的 RTSP 能力。
2. 用它验证 FFmpeg 协议能力。
3. 用它承诺截图、录制、缓存统计。

## 七、后续落地顺序

1. v2.9 完成接口和 fallback 原型。
2. 下一阶段 Demo 增加后端选择 UI。
3. 再决定是否引入 ExoPlayer。
4. 最后再考虑跨平台 core 和 iOS 工程。
