# ECHPlay v1.9 需求目标分解文档

版本定位：v1.9 聚焦 MediaPlayer-like API 对齐。目标不是一次性重写 Native 播放内核，而是先让 Java SDK 的接入方式更接近 ijkplayer / Android MediaPlayer，方便后续 v2.0-v2.1 继续扩展 option、property、数据源和协议能力。

一句话目标：

v1.9 完成后，业务侧可以用更接近 `IMediaPlayer` 的方式接入 ECHPlay，包括 `SurfaceHolder`、`Context + Uri`、headers、音量、循环、seek 完成回调和 TimedText 占位接口。

============================================================
一、需求拆解
============================================================

需求 1：新增 MediaPlayer-like 接口

目标：

1. 新增 `IECHMediaPlayer` 接口。
2. 接口命名参考 ijkplayer `IMediaPlayer`。
3. 覆盖常用播放控制：setDataSource、setDisplay、setSurface、prepareAsync、start、pause、stop、reset、release、seekTo、getDuration、getCurrentPosition。
4. 覆盖状态查询：isPlaying、isLooping、getVideoWidth、getVideoHeight、getTrackInfo。
5. 覆盖监听器设置：prepared、completion、error、info、buffering、video size、seek complete、timed text。

验收标准：

1. `ECHPlayer` 实现 `IECHMediaPlayer`。
2. 旧 API 保持兼容。
3. 新接口和方法有中文注释。

建议提交：

`新增MediaPlayer风格接口 / add MediaPlayer-like interface`

------------------------------------------------------------

需求 2：补齐数据源 API 形态

目标：

1. 增加 `setDataSource(Context, Uri)`。
2. 增加 `setDataSource(Context, Uri, Map<String, String> headers)`。
3. 增加 `setDataSource(String, Map<String, String> headers)`。
4. 增加 `setDataSource(FileDescriptor fd)`。
5. 增加 `getDataSource()`。
6. headers 先保存到 Java 层，并将 HTTP headers 转换为 FFmpeg `headers` option。
7. `content://` 和 `FileDescriptor` 的深度 Native 支持放到 v2.1，v1.9 先提供清晰异常和后续入口。

验收标准：

1. `file://` 和普通 path 能转换为现有播放地址。
2. `http://` / `https://` headers 能通过 option 写入。
3. 暂不支持的数据源类型报错清晰。

建议提交：

`补齐播放器数据源API / complete player data source APIs`

------------------------------------------------------------

需求 3：补齐显示、音量和循环 API

目标：

1. 增加 `setDisplay(SurfaceHolder holder)`。
2. `setDisplay(null)` 时清空 Surface。
3. 增加 `setVolume(float leftVolume, float rightVolume)`。
4. Java AudioTrack 输出时应用左右声道音量。
5. 增加 `setLooping(boolean looping)` 和 `isLooping()`。
6. 播放完成时如果 looping=true，则自动 seek 到 0 并重新 start。

验收标准：

1. 使用 `SurfaceHolder` 可以正常绑定 Surface。
2. 设置音量不会崩溃，并能影响 Java AudioTrack 输出。
3. 本地可 seek 文件播放结束后可循环播放。

建议提交：

`补齐显示音量和循环API / complete display volume and looping APIs`

------------------------------------------------------------

需求 4：补齐 seek 完成和 TimedText 占位

目标：

1. 增加 `OnSeekCompleteListener`。
2. seek 成功时分发独立回调。
3. 增加 `TimedText` 数据类型。
4. 增加 `OnTimedTextListener`。
5. v1.9 暂不解码字幕，只保留 API 占位，真实字幕放到 v2.5。

验收标准：

1. seek 成功后能收到 `onSeekComplete`。
2. TimedText API 可被业务侧编译引用。
3. 文档明确字幕功能暂未实现。

建议提交：

`新增seek完成和字幕占位接口 / add seek complete and timed text placeholder APIs`

------------------------------------------------------------

需求 5：验证和完成报告

目标：

1. 执行 Android 构建或 smoke check。
2. 新增 `v1.9_validation_report.md`。
3. 记录已完成 API、验证命令和已知边界。

验收标准：

1. `:echplayer:assembleDebug` 通过。
2. 如环境允许，执行 `android/tools/smoke_check.sh`。
3. 文档记录 v1.9 的边界：FileDescriptor / content Uri 深度播放在 v2.1 继续。

建议提交：

`完成v1.9验证报告 / add v1.9 validation report`

============================================================
二、完成定义
============================================================

v1.9 完成时必须满足：

1. `v1.9_requirements_goals.md` 存在。
2. `IECHMediaPlayer` 存在。
3. `ECHPlayer` 实现 `IECHMediaPlayer`。
4. `ECHPlayer` 支持 SurfaceHolder、Uri、headers、音量、循环、seek complete、TimedText 占位 API。
5. `v1.9_validation_report.md` 存在。
6. Android library 构建通过。

============================================================
三、风险与边界
============================================================

1. v1.9 不直接复制 ijkplayer 源码，只参考 API 形态。
2. v1.9 不实现真正字幕解码，字幕放到 v2.5。
3. v1.9 不完整实现 FileDescriptor / content Uri Native 播放，深度数据源能力放到 v2.1。
4. v1.9 的循环播放依赖当前媒体支持 seek，RTSP 等不可 seek 流不会强制循环。
