# v2.3 验证报告

## 已实现

1. 新增 `OnMediaCodecSelectListener`。
2. 新增 `INFO_MEDIACODEC_FORMAT_CHANGED`。
3. 新增 `mediacodec-auto-rotate` 和 `mediacodec-handle-resolution-change` option。
4. 新增 `mediacodec-avc` 和 `mediacodec-hevc` option，并在 native 硬解前生效。
5. 新增 `mediacodec-mpeg4` 和 `mediacodec-mpeg2` 预留 option，当前只接受关闭值。
6. Native 硬解创建成功后会询问 Java selector，拒绝时回退软解。
7. MediaCodec 输出格式变化时通知 Java 刷新视频尺寸。

## 编译检查

已通过：`cd android && ./tools/smoke_check.sh`，Debug/Release AAR 和 Demo Debug APK 构建成功。

## 手工验证建议

1. 播放 H.264 MP4，设置 `DECODE_MODE_MEDIACODEC`，确认能收到 `INFO_MEDIACODEC_OPENED`。
2. 设置 `mediacodec-avc=0` 后播放 H.264，确认回退软解。
3. 设置 selector 固定返回 false，确认回退软解并带有 selector 拒绝信息。
4. 播放 H.265 流，验证 `mediacodec-hevc` 开关对硬解路径生效。

## 已知边界

1. `mediacodec-auto-rotate` 当前已进入配置系统，但实际旋转显示仍依赖后续 metadata 解析和渲染矩阵。
2. MPEG2/MPEG4 硬解仍关闭，避免缺少 CSD/packet 转换导致播放不稳定。
3. TextureView 渲染入口尚未加入，仍以 Surface/OpenGL/NativeWindow 为主。
