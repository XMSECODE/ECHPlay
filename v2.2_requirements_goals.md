# v2.2 缓冲、队列和 seek 状态机目标

## 版本定位

v2.2 聚焦播放器运行态可观测性和 seek/缓冲体验。当前阶段不激进重写 native packet queue，先把 ijkplayer 常用的 seek option、缓冲水位和统计 property 接入，为后续 frame queue 和弱网恢复打基础。

## 目标拆解

1. seek 状态机

   - 保留 `SEEKING` 状态，避免重复 seek 并发进入 native。
   - 新增 `accurate_seek` option，作为后续 native 精确 seek 的统一入口。
   - 每次 seek 记录耗时，暴露到 `PROP_INT64_LATEST_SEEK_LOAD_DURATION`。
   - seek 成功时分发 `INFO_SEEK_COMPLETE`，消息中带 `accurateSeek` 和 `seekLoadDurationMs`。

2. 缓冲水位

   - 新增 `buffering_start_percent` option。
   - 新增 `buffering_end_percent` option。
   - Native 缓冲开始/结束事件映射到可配置水位。
   - seek 开始/结束同样分发缓冲水位，方便 UI 有稳定反馈。

3. 队列和属性观测

   - 继续保留 video/audio packet queue size property。
   - 继续保留网络读取速度和总读取字节数 property。
   - 将最近 seek 耗时从默认值升级为真实统计值。

4. 状态清理

   - `reset()` 重置 accurate seek、水位和 seek 耗时。
   - `release()` 清理 seek 耗时和最近缓冲百分比。

## 达成标准

1. 新增 option/property 均有中文注释。
2. `isOptionSupported()` 能识别 v2.2 新增 option。
3. seek 成功后能通过 property 读取最近 seek 耗时。
4. 编译 smoke check 通过。

## 后续边界

v2.2 不重写 native frame queue、flush packet 和多 clock 同步。那些属于高风险 native 调度改造，需要在已有水位和统计入口稳定后继续拆小步完成。
