# v2.1 数据源和协议能力增强目标

## 版本定位

v2.1 对齐 ijkplayer 在数据源和协议入口上的常用能力，让 ECHPlayer 能覆盖字符串地址、Uri、content Uri、FileDescriptor、基础自定义数据源、headers 和 RTSP 常用网络参数。

## 目标拆解

1. 数据源入口增强

   - 支持 `setDataSource(Context, Uri)` 处理 `content://`。
   - 支持 `setDataSource(FileDescriptor)` 播放调用方保持打开的 fd。
   - 新增 `setDataSource(ParcelFileDescriptor)`，播放器负责在 `reset/release` 时关闭 fd。
   - 新增 `ECHMediaDataSource`，提供类似 MediaDataSource 的 `getSize/readAt/close` 入口。

2. 协议配置增强

   - 保留并验证 headers 注入到 FFmpeg `headers` option。
   - 保留并验证 `protocol_whitelist` 注入。
   - 保留并验证 `user_agent` 注入。
   - 新增 `stimeout` option，和 `timeout` 一起映射到 RTSP 建连超时。
   - 继续支持 `rw_timeout`、`buffer_size`、`max_delay`、`rtsp_transport`。

3. 资源生命周期

   - content Uri 打开的 fd 在播放期间保持有效。
   - 新设置数据源、`reset()`、`release()` 时关闭播放器持有的 fd。
   - 自定义数据源复制到缓存文件后自动调用 `close()`。

4. 兼容性文档

   - 更新兼容性矩阵，标记 v2.1 新增能力。
   - 明确自定义数据源当前采用临时文件桥接，不等价于 ijkplayer native AVIO hook。

## 达成标准

1. Java 接口可编译，新增方法都有中文注释。
2. Native 能识别 `stimeout` 并在 RTSP 打开输入时下发。
3. `content://` 和 `ParcelFileDescriptor` 数据源不会在 prepare 前被提前关闭。
4. `./tools/smoke_check.sh` 能通过编译/语法检查。

## 后续边界

v2.1 不直接实现 ijkplayer 的完整 `ijkio` / `AVIOContext` 自定义协议 hook。当前先提供稳定易懂的文件桥接能力，真正的 native streaming data source 留给后续 cache/protocol hook 深化版本。
