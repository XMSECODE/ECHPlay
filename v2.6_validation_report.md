# v2.6 验证报告

## 已实现

1. 新增 `CacheStats`。
2. 新增 `cache_enable`、`cache_dir`、`cache_max_bytes` option。
3. 新增 cache 相关 property 入口。
4. 新增 `OnNativeInvokeListener`。
5. info 和重连关键事件会同步分发 native invoke 风格事件。
6. 新增 `reconnectImmediately()` 手动立即重连入口。
7. `getCacheStats()` 能返回流量、速度、缓存配置和最近 seek 耗时。

## 编译检查

已通过：`cd android && ./tools/smoke_check.sh`，Debug/Release AAR 和 Demo Debug APK 构建成功。

## 手工验证建议

1. 设置 `cache_enable=1`、`cache_dir` 和 `cache_max_bytes`，读取 `getCacheStats()`。
2. 播放网络流，观察 `trafficBytes` 和 `tcpSpeedBytesPerSecond`。
3. 设置 `OnNativeInvokeListener`，确认 info 事件能进入 invoke 回调。
4. RTSP 播放失败或手动调用 `reconnectImmediately()` 时观察重连 invoke 事件。

## 已知边界

1. 当前 cache 统计是基于读取字节数的估算，不是实际落盘 HTTP cache。
2. 真正的 async/cache 协议 hook 尚未实现。
3. 手动重连仍复用现有自动重连状态机。
