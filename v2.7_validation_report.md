# v2.7 验证报告

验证日期：2026-06-18

## 一、目标完成情况

| 目标 | 结果 | 说明 |
| --- | --- | --- |
| Maven Local 发布入口 | 已完成 | `echplayer` 增加 `maven-publish`，新增 `android/tools/publish_maven_local.sh` |
| ABI 真实状态文档 | 已完成 | 新增 `abi_release_status.md`，只声明 `arm64-v8a` 已支持 |
| Demo ijkplayer-like 设置摘要 | 已完成 | 主 Demo 展示数据源、RTSP、渲染、解码、网络默认参数和 ABI 摘要 |
| 发布模板 | 已完成 | 新增 `github_release_template.md` |
| README 接入说明 | 已完成 | 补充 Maven Local 集成和 ABI 状态说明 |
| 发版清单 | 已完成 | 增加 Maven Local、ABI 状态和 License 检查项 |

## 二、构建验证

待执行命令：

```bash
cd android
./tools/smoke_check.sh
```

预期结果：

1. `echplayer-debug.aar` 生成。
2. `echplayer-release.aar` 生成。
3. `app-debug.apk` 生成。
4. Release AAR 包含 `jni/arm64-v8a/libechplayer.so`。
5. Release AAR 包含 `arm64-v8a` FFmpeg so。

实际结果：

1. 已通过，`BUILD SUCCESSFUL`。
2. `echplayer-debug.aar`、`echplayer-release.aar`、`app-debug.apk` 均已生成。
3. Release AAR 已检查到 `jni/arm64-v8a/libechplayer.so`。
4. Release AAR 已检查到 `jni/arm64-v8a/libavcodec.so`、`libavformat.so`、`libavutil.so`、`libswresample.so`、`libswscale.so`。
5. 当前没有 adb 设备，安装和启动 Demo 被 smoke 脚本自动跳过。

## 三、Maven Local 验证

待执行命令：

```bash
cd android
./tools/publish_maven_local.sh
```

预期结果：

1. Gradle 执行 `:echplayer:publishReleasePublicationToMavenLocal` 成功。
2. Maven Local 出现 `com.echplay:echplayer:2.7.0`。
3. 发布过程不影响原有 AAR 构建。

实际结果：

1. 已通过，`BUILD SUCCESSFUL`。
2. `:echplayer:publishReleasePublicationToMavenLocal` 执行成功。
3. 已发布 `com.echplay:echplayer:2.7.0`。
4. 首次在沙箱内执行时被 `~/.gradle` 锁文件权限拦截，提升权限后发布成功。

## 四、ABI 验证

| ABI | v2.7 结论 | 依据 |
| --- | --- | --- |
| `arm64-v8a` | 已支持 | `jniLibs/arm64-v8a` 包含完整 FFmpeg so，smoke check 检查 AAR 条目 |
| `armeabi-v7a` | 待补齐 | 当前只有预留说明，没有完整 FFmpeg so |
| `x86_64` | 待预研 | 当前未接入目录和 so |
| `x86` | 暂不建议 | 低优先级，后续按实际设备需求决定 |

## 五、Demo 验证点

1. 首次打开主 Demo，能看到 ijkplayer-like 设置摘要。
2. 切换 RTSP TCP / UDP 后，摘要实时更新。
3. 切换渲染模式后，摘要实时更新并保存偏好。
4. 切换解码模式后，摘要实时更新并保存偏好。
5. 点击播放后，日志包含当前设置摘要。

## 六、风险和后续

1. v2.7 没有补齐 `armeabi-v7a` 和 `x86_64`，这是真实资源缺口，不是代码遗漏。
2. Maven Local 只是本机发布方式，后续如果要对团队或 CI 分发，需要接入远端 Maven 仓库。
3. AAR 包含 FFmpeg 动态库，正式公开发版前需要再次确认 FFmpeg 编译配置和 License 文档。
