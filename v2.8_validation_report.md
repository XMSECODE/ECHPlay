# v2.8 验证报告

验证日期：2026-06-18

## 一、目标完成情况

| 目标 | 结果 | 说明 |
| --- | --- | --- |
| smoke check 扩展 | 已完成 | 增加关键 API、自动回归入口和发版文档检查 |
| adb 自动回归入口 | 已完成 | `MainActivity` 支持播放地址、模式、RTSP、渲染、解码、截图、短录制参数 |
| 诊断脚本 | 已完成 | 新增 `android/tools/collect_diagnostics.sh` |
| 测试矩阵 | 已完成 | 新增 `v2.8_test_matrix.md` |
| 长播模板 | 已完成 | 新增 `long_play_test_template.md` |
| 发版文档更新 | 已完成 | README、兼容性矩阵、release checklist 已更新 |

## 二、待执行验证

```bash
cd android
./tools/smoke_check.sh
```

预期结果：

1. Debug / Release AAR 和 Demo APK 构建成功。
2. AAR 包含 `arm64-v8a` 播放器 so 和 FFmpeg so。
3. 关键 API 文本检查通过。
4. v2.8 文档检查通过。
5. 无 adb 设备时跳过安装启动。

实际结果：

1. 已通过，`BUILD SUCCESSFUL`。
2. `echplayer-debug.aar`、`echplayer-release.aar`、`app-debug.apk` 均已生成。
3. Release AAR 已检查 `jni/arm64-v8a/libechplayer.so` 和 FFmpeg so。
4. `captureCurrentFramePng`、`startRecording`、`setRtspTransport`、`getPropertyLong` 关键 API 文本检查通过。
5. `EXTRA_AUTO_PLAY` 和 `scheduleAutomationActions` 自动回归入口检查通过。
6. v2.8 测试矩阵和验证报告文档检查通过。
7. 当前环境无可用 adb 设备，安装启动被 smoke 脚本跳过。

## 三、诊断脚本验证

```bash
cd android
./tools/collect_diagnostics.sh
```

预期结果：

1. 无 adb 时输出 `adb not found` 并退出 0。
2. 无设备时输出 `no adb device` 并退出 0。
3. 有设备时输出目录在 `android/build/diagnostics/`。

实际结果：

1. 已通过。
2. 当前环境 adb daemon 启动被系统限制，脚本输出 `adb unavailable, skip diagnostics` 并退出 0。
3. 该结果符合“诊断收集不能阻塞无设备或受限环境发版”的设计目标。

## 四、adb 自动回归参数

示例：

```bash
adb shell am start \
  -n com.example.abcplaydemo/.MainActivity \
  --ez echplay_auto_play true \
  --es echplay_source "rtsp://120.24.161.118:8554/live" \
  --es echplay_source_mode network \
  --es echplay_rtsp_transport tcp \
  --es echplay_render_mode auto \
  --es echplay_decode_mode auto \
  --el echplay_auto_capture_delay_ms 3000 \
  --el echplay_auto_record_duration_ms 5000
```

验证说明：

1. 当前环境没有 adb 设备时，只验证编译和参数代码路径。
2. 后续接入真机后，按 `v2.8_test_matrix.md` 补齐协议和长播结果。

## 五、风险和后续

1. v2.8 仍不是完整 CI 系统，只是本地发版前自动化护栏。
2. 自动播放能启动 Demo，但协议是否真正可播仍依赖设备、源和网络。
3. 长播测试需要真实设备和 2 小时时间窗口，当前只提供模板和流程。
