# v2.2 验证报告

## 已实现

1. 新增 `OPTION_ACCURATE_SEEK`。
2. 新增 `OPTION_BUFFERING_START_PERCENT` 和 `OPTION_BUFFERING_END_PERCENT`。
3. `PROP_INT64_LATEST_SEEK_LOAD_DURATION` 返回最近一次 seek 耗时。
4. seek 期间分发缓冲开始/结束水位，失败时恢复 seek 前缓冲百分比。
5. Native 缓冲开始/结束事件使用可配置水位分发给业务层。
6. `reset()` 和 `release()` 清理 v2.2 新增状态。

## 编译检查

已通过：`cd android && ./tools/smoke_check.sh`，Debug/Release AAR 和 Demo Debug APK 构建成功。

## 手工验证建议

1. 播放本地 MP4，调用 `setOption(OPTION_CATEGORY_PLAYER, OPTION_ACCURATE_SEEK, 1)` 后 seek。
2. seek 完成后读取 `PROP_INT64_LATEST_SEEK_LOAD_DURATION`，确认返回非负值。
3. 设置 `buffering_start_percent=10`、`buffering_end_percent=90`，确认 UI 缓冲回调按新水位显示。
4. 连续快速 seek，确认第二次并发 seek 会被拒绝而不是进入 native 并发。

## 已知边界

1. `accurate_seek` 当前是 Java 层配置入口，native 仍使用现有 FFmpeg seek 策略。
2. Native packet queue 仍是现有队列实现，尚未完全对齐 ijkplayer frame queue/flush packet 模型。
3. 弱网恢复仍依赖 v1.x 自动重连能力，v2.2 只强化缓冲可观测性。
