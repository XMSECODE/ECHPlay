# v2.6 缓存、统计和 Native invoke 目标

## 版本定位

v2.6 对齐 ijkplayer 的缓存统计、流量统计和 native invoke 扩展事件入口。当前阶段先建立统一 API 和可观测指标，后续再替换为真正的 HTTP 本地缓存实现。

## 目标拆解

1. 缓存配置

   - 新增 `cache_enable` option。
   - 新增 `cache_dir` option。
   - 新增 `cache_max_bytes` option。
   - 新增 `CacheStats` 统计快照。

2. 统计属性

   - 复用网络读取总字节数作为 `trafficBytes`。
   - 复用网络读取速度作为 `tcpSpeedBytesPerSecond`。
   - 暴露 `latestSeekLoadDurationMs`。
   - 新增 cache forwards/file pos/physical pos 属性入口。

3. Native invoke 风格事件

   - 新增 `OnNativeInvokeListener`。
   - info 事件通过 invoke 入口同步分发。
   - 自动重连开始、失败通过 invoke 入口分发。

4. 主动重连

   - 新增 `reconnectImmediately()`。
   - 可对当前 RTSP 数据源立即触发一次重连流程。

## 达成标准

1. 新增类型、属性、方法均有中文注释。
2. cache option 能进入 option snapshot。
3. `getCacheStats()` 返回可读统计快照。
4. 编译 smoke check 通过。

## 后续边界

本版本不实现真正的 HTTP 文件缓存和协议 hook，cache forwards/file pos/physical pos 当前基于读取字节数估算。完整 cache file 和 async cache 协议需要后续 native IO 层继续实现。
