# v2.9 验证报告

验证日期：2026-06-18

## 一、目标完成情况

| 目标 | 结果 | 说明 |
| --- | --- | --- |
| PlayerBackend 接口 | 已完成 | 抽象播放控制最小公约数 |
| ECHPlayerBackend | 已完成 | 包装 ECHPlayer 主后端 |
| AndroidMediaPlayerBackend | 已完成 | 提供系统 MediaPlayer fallback 原型 |
| PlayerBackendFactory | 已完成 | 集中创建默认后端和 fallback 后端 |
| 后端决策文档 | 已完成 | `v2.9_backend_decision.md` |
| 跨平台预研文档 | 已完成 | `v2.9_cross_platform_research.md` |
| smoke check 扩展 | 已完成 | 增加后端接口和 v2.9 文档检查 |

## 二、待执行验证

```bash
cd android
./tools/smoke_check.sh
```

预期结果：

1. Debug / Release AAR 和 Demo APK 构建成功。
2. AAR 包含 `arm64-v8a` 播放器 so 和 FFmpeg so。
3. v2.8 的关键 API 和自动回归入口检查继续通过。
4. `PlayerBackend` 和 `PlayerBackendFactory` 文本检查通过。
5. v2.9 后端决策和验证报告文档检查通过。

实际结果：

1. 已通过，`BUILD SUCCESSFUL`。
2. `echplayer-debug.aar`、`echplayer-release.aar`、`app-debug.apk` 均已生成。
3. Release AAR 已检查 `jni/arm64-v8a/libechplayer.so` 和 FFmpeg so。
4. v2.8 的截图、录制、RTSP、property、自动回归入口检查继续通过。
5. `PlayerBackend` 和 `PlayerBackendFactory` 文本检查通过。
6. `v2.9_backend_decision.md` 和 `v2.9_validation_report.md` 文档检查通过。
7. smoke check 检测到 adb 设备，已成功安装并启动 Demo。

## 三、后端能力验证范围

| 后端 | 编译验证 | 运行验证 | 说明 |
| --- | --- | --- | --- |
| ECHPlayerBackend | 已通过编译 | 待真机专项验证 | 包装现有 ECHPlayer，不改变主线行为 |
| AndroidMediaPlayerBackend | 已通过编译 | 待真机专项验证 | 只作为系统播放器 fallback 原型 |
| ExoPlayerBackend | 不适用 | 不适用 | v2.9 只做决策，不引入依赖 |
| iOS backend | 不适用 | 不适用 | v2.9 只做预研，不实现工程 |

## 四、风险和后续

1. `PlayerBackend` 是最小公约数接口，不包含 ECHPlayer 的扩展能力。
2. Android MediaPlayer fallback 不支持 RTSP、截图、录制和 FFmpeg property。
3. Demo 还没有后端选择 UI，后续版本可在不破坏主线的前提下接入。
4. iOS 需要先拆平台无关 core，不应在当前阶段直接开工程。
