# ECHPlay v1.2 需求目标分解文档

版本定位：v1.2 聚焦“工程化和 library module”。这一版不继续堆播放器功能，而是把 v1.1 已经完成的 ECHPlayer、ECHPlayerView、NativePlayer、FFmpeg so、JNI 等能力，从 app Demo 中拆出来，变成可以被其他 Android 项目复用的播放器库。

参考依据：

1. v1.1 已完成播放器 API、状态机、回调错误码、RTSP、seek、截图、录制、PlayerView 和 README。
2. ijkplayer 的工程形态具备独立播放器库、多 ABI so、Gradle 依赖、Demo App 边界。
3. 当前 ECHPlay 仍是单一 `app` module，播放器代码、Demo 页面、native 代码、FFmpeg so 混在一起。

============================================================
一、v1.2 版本主题
============================================================

v1.2 主题：播放器工程化第一步。

核心目标：

1. 新增独立 Android library module：`echplayer`。
2. app module 只保留 Demo 页面和示例资源。
3. ECHPlayer、ECHPlayerView、JNI、NativePlayer、FFmpeg 依赖迁移到 `echplayer`。
4. 能输出 Debug / Release AAR。
5. 支持至少 arm64-v8a 和 armeabi-v7a 两种 ABI 的打包结构。
6. README 增加 library module 和 AAR 集成说明。

一句话标准：

业务方不需要复制 MainActivity 或 native 代码，只要依赖 `echplayer` module 或 AAR，就能使用 ECHPlayer / ECHPlayerView。

============================================================
二、v1.2 不做什么
============================================================

以下内容不进入 v1.2 必须完成范围：

1. 不做 OpenGL ES 渲染。
2. 不做 MediaCodec 硬解。
3. 不做 HTTP / HLS / 自动重连专项。
4. 不做 Maven Central 或私服正式发布。
5. 不做 iOS。
6. 不重写 NativePlayer 架构。
7. 不改变 v1.1 已有播放器 API 行为。

v1.2 的重点是拆分、复用、打包和验证。

============================================================
三、当前工程现状
============================================================

当前 Gradle 结构：

1. `android/settings.gradle` 只包含 `:app`。
2. `android/app/build.gradle` 使用 `com.android.application`。
3. `android/app/src/main/java` 同时包含 Demo 页面和播放器类。
4. `android/app/src/main/cpp` 包含 JNI、NativePlayer、FFmpeg include。
5. `android/app/src/main/jniLibs/arm64-v8a` 包含 FFmpeg so。
6. `android/app/src/main/assets/test.mp4` 是 Demo 测试资源。

需要拆分的播放器代码：

1. `com.example.abcplaydemo.player.ECHPlayer`
2. `com.example.abcplaydemo.player.ECHPlayerView`
3. `android/app/src/main/cpp`
4. `android/app/src/main/jniLibs`

需要留在 Demo app 的代码：

1. `MainActivity`
2. `PlayerViewDemoActivity`
3. Demo layout
4. Demo assets
5. Demo app icon / theme / manifest

============================================================
四、v1.2 需求分解
============================================================

需求 1：新增 `echplayer` Android library module

目标：

1. 在 `android/settings.gradle` 中加入 `:echplayer`。
2. 新建 `android/echplayer/build.gradle`。
3. 使用 `com.android.library` 插件。
4. 配置 namespace，例如 `com.echplay.player`。
5. 统一 compileSdk、minSdk、Java 版本。

验收标准：

1. `./gradlew :echplayer:assembleDebug` 能编译通过。
2. `./gradlew :echplayer:assembleRelease` 能编译通过。
3. `android/echplayer/build/outputs/aar/` 能生成 AAR。

============================================================

需求 2：迁移 Java 播放器 API 到 library module

目标：

1. 将 `ECHPlayer.java` 移到 `echplayer/src/main/java`。
2. 将 `ECHPlayerView.java` 移到 `echplayer/src/main/java`。
3. 包名建议调整为 `com.echplay.player`。
4. Demo app 通过 import 使用 library module。
5. 所有公开类、方法、属性继续保留中文注释。

验收标准：

1. app module 中不再保存播放器 SDK 类。
2. `MainActivity` 和 `PlayerViewDemoActivity` 从 `com.echplay.player` 导入播放器。
3. v1.1 已有 API 名称不破坏：`ECHPlayer`、`ECHPlayerView` 仍可使用。

============================================================

需求 3：迁移 native 播放器核心到 library module

目标：

1. 将 `app/src/main/cpp` 迁移到 `echplayer/src/main/cpp`。
2. `echplayer/build.gradle` 配置 externalNativeBuild。
3. CMake 输出 so 名称保持稳定，例如 `abcplaydemo` 或调整为 `echplayer`。
4. Java `System.loadLibrary(...)` 与 CMake 输出保持一致。
5. JNI 方法包名同步调整。

验收标准：

1. `ECHPlayer` 能正常加载 native so。
2. JNI 方法签名与新包名匹配。
3. 本地 MP4 能播放。
4. RTSP 能播放或正常返回错误。

风险提示：

1. 如果修改 Java 包名，JNI 函数名必须同步修改。
2. 如果修改 native so 名称，`System.loadLibrary` 必须同步修改。
3. 此需求是 v1.2 中风险最高的拆分点。

============================================================

需求 4：迁移 FFmpeg include 和 so 到 library module

目标：

1. 将 FFmpeg include 放到 `echplayer/src/main/cpp/ffmpeg/include`。
2. 将 FFmpeg so 放到 `echplayer/src/main/jniLibs`。
3. app module 不再直接包含 FFmpeg so。
4. AAR 内能带上播放器 native so 和 FFmpeg so。

验收标准：

1. app 打包后 APK 内包含 echplayer native so 和 FFmpeg so。
2. `:echplayer:assembleRelease` 生成的 AAR 内包含 jni so。
3. 运行时不出现 `UnsatisfiedLinkError`。

============================================================

需求 5：app module 改为纯 Demo

目标：

1. app module 依赖 `implementation project(':echplayer')`。
2. app module 保留 MainActivity 和 PlayerViewDemoActivity。
3. app module 保留 assets/test.mp4。
4. app module 不再配置 externalNativeBuild。
5. app module 不再直接持有播放器 native 和 FFmpeg so。

验收标准：

1. `./gradlew :app:assembleDebug -x test` 编译通过。
2. MainActivity 本地播放入口可用。
3. MainActivity RTSP 入口可用。
4. PlayerViewDemoActivity 可打开并通过 ECHPlayerView 播放。

============================================================

需求 6：多 ABI 打包结构

目标：

1. 保留 arm64-v8a。
2. 增加 armeabi-v7a 的目录结构。
3. 如果当前没有 armeabi-v7a FFmpeg so，文档中明确标记为待补齐外部依赖。
4. Gradle abiFilters 至少预留 arm64-v8a、armeabi-v7a。
5. README 说明当前 ABI 支持状态。

验收标准：

1. arm64-v8a 能打包和运行。
2. armeabi-v7a 的 Gradle 配置完成。
3. 如果缺少 armeabi-v7a so，不静默伪装完成，必须在 README 和文档里标注。

说明：

v1.2 可以把“多 ABI 工程结构”作为必做，把“armeabi-v7a FFmpeg so 实际可运行”作为依赖外部 so 的验收项。

============================================================

需求 7：AAR 输出和本地集成说明

目标：

1. 支持 `./gradlew :echplayer:assembleRelease`。
2. 生成 Release AAR。
3. README 增加 project module 集成方式。
4. README 增加本地 AAR 集成方式。
5. README 增加最小使用示例。

验收标准：

1. 能找到 `echplayer-release.aar`。
2. 新人能按 README 使用 `implementation project(':echplayer')`。
3. 新人能按 README 使用本地 AAR。

============================================================

需求 8：包名、命名和版本信息整理

目标：

1. library namespace 建议使用 `com.echplay.player`。
2. app namespace 可继续保持 `com.example.abcplaydemo`。
3. library versionName 设为 `1.2.0`。
4. app versionName 可同步设为 `1.2.0-demo` 或 `1.2.0`。
5. 文档记录版本号策略。

验收标准：

1. library 和 app namespace 边界清晰。
2. README 能看出当前播放器库版本。
3. Demo app 不再被误认为 SDK 包名。

============================================================

需求 9：回归测试清单

目标：

1. 保留 v1.1 所有功能回归。
2. 新增 module 拆分专项测试。
3. 新增 AAR 输出测试。
4. 新增 APK so 包含检查。

验收标准：

1. `./gradlew :echplayer:assembleDebug` 通过。
2. `./gradlew :echplayer:assembleRelease` 通过。
3. `./gradlew :app:assembleDebug -x test` 通过。
4. APK 中能看到播放器 so 和 FFmpeg so。
5. Demo 本地播放、RTSP、截图、录制、seek、release 可用。

============================================================
五、推荐实施顺序
============================================================

步骤 1：创建 `echplayer` module

产出：

1. `android/echplayer/build.gradle`
2. `android/echplayer/src/main/AndroidManifest.xml`
3. `settings.gradle` include `:echplayer`

建议提交：

`新增播放器library模块 / add player library module`

============================================================

步骤 2：迁移 Java 播放器类

产出：

1. `ECHPlayer` 迁入 library。
2. `ECHPlayerView` 迁入 library。
3. app 更新 import。

建议提交：

`迁移Java播放器API到library / move Java player API to library`

============================================================

步骤 3：迁移 native、CMake、FFmpeg so

产出：

1. native 代码迁入 library。
2. CMake 配置迁入 library。
3. jniLibs 迁入 library。
4. JNI 签名和 so 加载修正。

建议提交：

`迁移native播放器核心到library / move native player core to library`

============================================================

步骤 4：app module 瘦身

产出：

1. app 去掉 externalNativeBuild。
2. app 去掉播放器 SDK 源码。
3. app 只依赖 `:echplayer`。

建议提交：

`瘦身Demo应用模块 / slim demo app module`

============================================================

步骤 5：AAR 和文档补齐

产出：

1. README 更新。
2. AAR 输出说明。
3. v1.2 测试清单。

建议提交：

`补齐v1.2集成文档 / add v1.2 integration docs`

============================================================
六、v1.2 验收标准总表
============================================================

必须通过：

1. `./gradlew :echplayer:assembleDebug`
2. `./gradlew :echplayer:assembleRelease`
3. `./gradlew :app:assembleDebug -x test`
4. app module 只保留 Demo，不再直接包含播放器 SDK 源码。
5. ECHPlayer / ECHPlayerView 从 library module 导入。
6. AAR 能生成。
7. 本地 MP4 可播放。
8. RTSP 可播放或能返回明确错误。
9. 截图保存解码帧 PNG。
10. 录制文件可生成并安全停止。
11. PlayerView Demo 可打开。

建议通过：

1. APK 内 so 检查。
2. Release AAR 解压检查 jni so。
3. README 按步骤能被新人复现。
4. arm64-v8a 真机或模拟器运行通过。

外部依赖项：

1. armeabi-v7a FFmpeg so 如果当前没有，需要额外编译或提供。
2. 如果短期无法提供 armeabi-v7a so，则 v1.2 文档必须明确“工程结构已预留，实际运行依赖 so 补齐”。

============================================================
七、v1.2 风险点
============================================================

风险 1：JNI 包名变化导致 native 方法找不到

规避：

1. 统一 Java 包名后同步修改 `native-lib.cpp` JNI 函数名。
2. 构建后必须实际启动播放验证。

风险 2：AAR 未打入 FFmpeg so

规避：

1. FFmpeg so 必须放在 library 的 `src/main/jniLibs`。
2. 解压 AAR 检查 `jni/arm64-v8a/*.so`。

风险 3：app 和 library 资源冲突

规避：

1. library 尽量少放资源。
2. PlayerView 如需资源，命名前缀使用 `echplayer_`。

风险 4：拆分过大导致功能回归

规避：

1. 分步骤提交。
2. 每步构建。
3. 最后完整跑 v1.1 回归清单。

============================================================
八、v1.2 完成定义
============================================================

v1.2 完成时，必须满足：

1. ECHPlayer 和 ECHPlayerView 已经属于 `echplayer` library module。
2. app module 只作为 Demo 使用播放器库。
3. `echplayer-release.aar` 可以生成。
4. app Debug APK 可以构建并运行。
5. v1.1 的核心播放能力没有回退。
6. README 包含 module 依赖和 AAR 集成方式。
7. v1.2 变更已提交代码。

============================================================
九、v1.2 完成后再进入的下一阶段
============================================================

v1.2 完成后，建议进入 v1.3：

1. OpenGL ES 渲染。
2. YUV 三纹理。
3. PlayerView 画面比例模式。
4. 视频尺寸变化回调。

原因：

library module 拆分完成后，再引入 OpenGL 渲染会更干净，渲染器可以成为播放器库内部能力，而不是继续堆在 Demo app 里。
