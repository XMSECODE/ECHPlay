# v2.9 跨平台预研文档

本文档用于判断 ECHPlay 是否应该向 iOS 和平台无关 core 演进。

## 一、当前结论

1. v2.9 不启动 iOS 工程。
2. v2.9 不拆 native core 为跨平台库。
3. 当前优先级仍是 Android 主线稳定、测试矩阵、RTSP 兼容性和发布质量。
4. iOS 可以进入后续版本规划，但必须先满足 FFmpeg license、渲染、音频、录制、CI 和示例工程条件。

## 二、与 ijkplayer 的差距

ijkplayer 有 Android 和 iOS 两条工程线，并且 iOS 侧具备独立播放器、FFmpeg 构建、渲染和音频输出体系。ECHPlay 当前只有 Android 工程，native 代码也强依赖 Android：

1. `ANativeWindow`。
2. Android `MediaCodec`。
3. Java `AudioTrack` 回调。
4. Android JNI 包名。
5. Android Gradle / CMake 打包。

这些依赖说明 ECHPlay 不能简单复制目录就变成 iOS SDK。

## 三、进入 iOS 的前置条件

1. Android 主播放器至少完成 2 小时长播矩阵。
2. RTSP TCP / UDP 在 Android 真机上有稳定结论。
3. FFmpeg 构建脚本和 License 文档稳定。
4. native core 拆出平台无关层，例如 demux、decode、packet queue、clock。
5. 渲染层拆出 Android / iOS 适配边界。
6. 音频层拆出 Android AudioTrack / iOS AudioUnit 或 AVAudioEngine 边界。
7. CI 能构建 Android AAR 和 iOS Framework。

## 四、建议的平台边界

建议后续拆成四层：

| 层级 | 职责 | Android 实现 | iOS 实现 |
| --- | --- | --- | --- |
| Core | demux、decode、queue、clock、seek | C++ | C++ |
| VideoOutput | 视频帧输出和渲染适配 | OpenGL ES / ANativeWindow / MediaCodec | Metal / OpenGL ES / VideoToolbox |
| AudioOutput | 音频输出 | AudioTrack / 后续 Native Audio | AudioUnit / AVAudioEngine |
| PlatformAdapter | 生命周期、Surface、文件、权限 | Java / JNI | Objective-C++ / Swift |

## 五、短期不做 iOS 的原因

1. 当前 Android RTSP 和长播稳定性还未完全闭环。
2. 现有 native 渲染和音频路径还没有平台无关抽象。
3. iOS 不是简单补文件，需要完整工程、签名、Framework、Demo 和测试矩阵。
4. 如果过早开启，会分散 Android 主线稳定性投入。

## 六、后续版本建议

1. v3.0：Demo 后端选择 UI 和 ExoPlayer 后端评估。
2. v3.1：native core 分层设计文档和 Android 侧无行为变化重构。
3. v3.2：iOS 工程可行性 spike，只验证 FFmpeg 打包、视频输出和本地 MP4。
4. v3.3：决定是否正式进入跨平台 SDK。

## 七、最终判断

iOS 应作为中长期目标，但不应该在 v2.9 承诺实现。v2.9 的正确交付是把后端接口边界先立住，让后续 ExoPlayer 和 iOS 都有清晰接入点。
