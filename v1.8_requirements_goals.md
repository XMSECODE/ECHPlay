# ECHPlay v1.8 需求目标分解文档

版本定位：v1.8 聚焦“测试体系和稳定性文档”。前面版本已经持续增加播放器能力，但验证仍大量依赖手动操作。v1.8 不追求新增大功能，而是建立可重复执行的 smoke 测试脚本、兼容性矩阵和发版检查清单。

一句话目标：

v1.8 完成后，每次发版前可以按固定脚本和固定文档检查构建产物、API 结构、Demo 安装、基础页面启动和已知协议状态。

============================================================
一、需求拆解
============================================================

需求 1：新增 smoke 测试脚本

目标：

1. 新增 `android/tools/smoke_check.sh`。
2. 脚本执行 Debug / Release 构建。
3. 检查 AAR 和 APK 产物是否存在。
4. 检查 AAR 内是否包含 arm64-v8a native so。
5. 可选：存在 adb 设备时安装 Demo APK 并启动主 Activity。

验收标准：

1. 无 adb 设备时脚本仍能完成构建和产物检查。
2. 有 adb 设备时可安装并启动 Demo。
3. 脚本失败时返回非 0。

建议提交：

`新增Android冒烟检查脚本 / add Android smoke check script`

============================================================

需求 2：新增兼容性矩阵文档

目标：

1. 新增 `compatibility_matrix.md`。
2. 记录 ABI、Android 版本、设备、协议、编码、解码方式、验证状态。
3. 将 v1.5 RTSP、HTTP、HLS 已知结果迁入矩阵。
4. 明确待验证项目和失败原因。

验收标准：

1. 文档能区分已通过、已执行未通过、待验证。
2. RTSP Android 侧失败原因如实保留。
3. HTTPS、HLS Live 等待验证项明确列出。

建议提交：

`新增兼容性矩阵 / add compatibility matrix`

============================================================

需求 3：新增发版检查清单

目标：

1. 新增 `release_checklist.md`。
2. 包含构建、产物、协议、截图、录制、PlayerView、文档、tag 推送检查项。
3. 检查项使用可勾选格式。
4. README 链接到发版清单。

验收标准：

1. 新版本发布前可直接按清单执行。
2. 清单覆盖 v1.5-v1.7 已有核心能力。
3. 清单明确哪些项目允许标为待验证。

建议提交：

`新增发版检查清单 / add release checklist`

============================================================

需求 4：最终回归报告

目标：

1. 新增 `v1.8_validation_report.md`。
2. 记录 smoke 脚本执行结果。
3. 记录构建产物检查结果。
4. 汇总 v1.6-v1.8 完成情况。

验收标准：

1. `android/tools/smoke_check.sh` 可执行并通过基础检查。
2. Debug / Release 构建通过。
3. v1.8 文档明确当前已知边界。

建议提交：

`补齐v1.8验证报告 / add v1.8 validation report`

============================================================
二、完成定义
============================================================

v1.8 完成时必须满足：

1. `android/tools/smoke_check.sh` 存在并可运行。
2. `compatibility_matrix.md` 存在。
3. `release_checklist.md` 存在。
4. `v1.8_validation_report.md` 存在。
5. README 能找到测试和发版文档入口。
6. smoke 脚本基础检查通过。

============================================================
三、风险与边界
============================================================

1. v1.8 不保证覆盖所有真机。
2. 长时间 2 小时播放测试先以文档入口预留，实际执行需要稳定测试源和设备。
3. adb 自动化只做可选增强，不作为无设备环境的硬依赖。
