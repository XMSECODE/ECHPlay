# ECHPlay v1.5 验证与回归记录

本文档记录 v1.5 网络播放、弱网恢复和播放统计版本的验证结果。v1.5 的验证原则是：代码路径存在不等于已通过，只有完成实际播放或明确错误验证后，才标记为通过。

============================================================
一、版本目标完成情况
============================================================

目标状态：

1. 协议能力表：已建立。
2. HTTP MP4 播放和 seek：播放已验证，seek 待复测。
3. HTTPS MP4 播放和 seek：待验证。
4. HLS / m3u8 基础播放：VOD 已验证。
5. RTSP TCP / UDP 真实源验证：已执行，Android 侧待兼容修复。
6. RTSP 自动重连：已开发，失败路径 smoke 已验证。
7. 网速、缓冲、队列长度统计：已开发，Debug 构建通过。
8. PlayerView loading / buffering / error / retry：已开发，Debug 构建通过。
9. README v1.5 网络播放说明：进行中。

============================================================
二、协议能力表
============================================================

| 协议 | 当前代码路径 | v1.5 目标 | 当前验证状态 | 备注 |
| --- | --- | --- | --- | --- |
| 本地文件 | 已支持 | 回归验证 | v1.4 已验证 | 需要确保 v1.5 不退化 |
| RTSP TCP | 已支持入口 | 真实源验证、断流重连 | 已执行，未通过 | mediamtx + 宿主机 FFmpeg 拉流通过，Android FFmpeg 打开失败 |
| RTSP UDP | 已支持入口 | 真实源验证、断流重连 | 已执行，未通过 | mediamtx 源可用，Android 真机 UDP 拉流超时 |
| HTTP MP4 | 已支持 | 播放、seek、错误诊断 | 播放已通过，seek 待复测 | 普通 MP4 需要 HTTP server 支持 Range，或使用 faststart MP4 |
| HTTPS MP4 | FFmpeg 可能支持 | 播放、seek、错误诊断 | 待验证 | 需要 HTTPS 测试源和证书兼容验证 |
| HLS VOD | 已支持 | m3u8 基础播放 | 已通过 | 不承诺加密、多码率切换 |
| HLS Live | FFmpeg 可能支持 | 播放或明确错误 | 待验证 | seek 预期可能不支持 |

============================================================
三、构建验证
============================================================

待执行：

```bash
cd android
./gradlew :echplayer:assembleDebug :app:assembleDebug -x test --console=plain
./gradlew :echplayer:assembleRelease -x test --console=plain
```

记录：

1. Debug 构建结果：通过，2026-06-13 执行 `./gradlew :echplayer:assembleDebug :app:assembleDebug -x test --console=plain`。
2. Release AAR 构建结果：待记录。
3. Gradle 警告：存在 Gradle 9.0 兼容性弃用提示和 `MainActivity` 过时 API 提示，当前不影响 Debug 构建。

============================================================
四、协议验证记录
============================================================

HTTP MP4：

1. 测试 URL：`http://10.0.2.2:18080/test_faststart_30s.mp4`，宿主机目录 `/tmp/echplay_v15_http` 通过 Python HTTP server 提供。
2. 编码格式：H.264 High Profile + AAC，时长约 30.18 秒，faststart MP4。
3. prepare 结果：通过，日志显示 `prepare success`、format 为 `mov,mp4,m4a,3gp,3g2,mj2`。
4. start 结果：通过，日志显示 `play started`、首帧 info 已回调。
5. seek 结果：Demo 已增加“跳中间”按钮调用 `player.seekTo(...)`，自动化 UI 抓取受 `uiautomator` idle state 影响，待复测。
6. 实际解码方式：AUTO 模式下进入 MediaCodec，日志显示 `decoder: video/avc`，完整播放解码 `decodedFrameCount=903`。
7. 错误或边界：同一素材的普通 MP4 在 Python SimpleHTTPServer 不支持 Range 时，FFmpeg 可 prepare 但 start 后 `av_read_frame` 返回 I/O error；HTTP MP4 建议使用 faststart，或使用支持 Range 的 HTTP server。

HTTPS MP4：

1. 测试 URL：
2. 编码格式：
3. prepare 结果：
4. start 结果：
5. seek 结果：
6. 实际解码方式：
7. 错误或边界：

HLS / m3u8：

1. 测试 URL：`http://10.0.2.2:18080/hls/index.m3u8`，由本地 MP4 通过 FFmpeg 切成 4 个 ts 分片。
2. 类型：VOD。
3. prepare 结果：通过，日志显示 `prepare success`。
4. start 结果：通过，日志显示 `play started`、demux / decode / audioDecode 线程启动。
5. seek 结果：待复测；v1.5 当前先确认 VOD m3u8 基础播放。
6. 实际解码方式：AUTO 模式下进入 MediaCodec，日志显示 `decoder: video/avc`，完整播放解码 `decodedFrameCount=301`。
7. 错误或边界：v1.5 只验证未加密单码率 HLS VOD；HLS Live、加密、多码率切换、字幕不在本轮承诺范围。

RTSP：

1. 测试源：本机 mediamtx 1.19.1，路径 `rtsp://127.0.0.1:8554/echplay` / `rtsp://10.0.2.2:8554/echplay`。
2. 传输方式：TCP / UDP 均已执行。
3. 编码格式：FFmpeg `testsrc` 实时编码 H.264 Constrained Baseline，640x360，25fps。
4. prepare 结果：宿主机 FFmpeg TCP 拉流通过；Android 模拟器 TCP 返回 `Invalid data found when processing input`；Android 真机 TCP 返回 `Invalid data found when processing input`；Android 真机 UDP 返回 `Connection timed out`。
5. start 结果：Android 侧未进入 start，失败发生在 `avformat_open_input`。
6. 实际解码方式：Android 侧未进入解码；宿主机 FFmpeg 可读取并解码 H.264。
7. 断流重连结果：Java API 和 Demo 已接入自动重连；使用不可达 RTSP 地址执行 smoke，日志显示按 2 秒间隔重试 3 次。因 Android 侧 RTSP 尚未成功播放，待 RTSP 兼容修复后复测真实断流恢复。
8. 错误或边界：本轮确认服务端和源流可用，但 Android 打包 FFmpeg 对 mediamtx RTSP H.264 源打开失败；后续需要继续排查 Android FFmpeg 构建参数、RTSP SDP 兼容性或更换真实摄像头源交叉验证。

============================================================
五、网络统计验证记录
============================================================

实现记录：

1. 累计读取字节数：已通过 `ECHPlayer.PlaybackStats.readBytes` 暴露。
2. 当前读取速度：已通过 `ECHPlayer.PlaybackStats.readSpeedBytesPerSecond` 暴露，Native 侧按约 1 秒窗口估算。
3. 视频 packet 队列长度：已通过 `ECHPlayer.PlaybackStats.videoPacketQueueSize` 暴露。
4. 音频 packet 队列长度：已通过 `ECHPlayer.PlaybackStats.audioPacketQueueSize` 暴露。
5. 缓冲百分比：已通过 `ECHPlayer.PlaybackStats.bufferedPercent` 暴露，当前按音视频 packet 队列占比做轻量估算。
6. prepare 耗时：暂未单独暴露为 API，后续可并入更完整的首开统计。
7. 首帧耗时：暂未单独暴露为 API，当前仍通过 `INFO_VIDEO_RENDERING_START` 和日志观察。
8. decode fps：已通过 `ECHPlayer.PlaybackStats.decodeFps` 暴露，当前为本轮播放的平均视频解码 FPS。
9. Demo 展示：主页面解码状态栏已周期刷新读取速度、累计读取字节、音视频队列长度、缓冲百分比、decode fps 和重连次数。
10. 构建结果：通过，2026-06-13 执行 `./gradlew :echplayer:assembleDebug :app:assembleDebug -x test --console=plain`。

============================================================
六、PlayerView 状态验证记录
============================================================

实现记录：

1. loading 状态：`ECHPlayerView.start()` 和 `INFO_PREPARE_STARTED` 会显示“正在准备播放...”。
2. buffering 状态：`INFO_BUFFERING_START` 或缓冲百分比 0 会显示“正在缓冲...”，缓冲结束后隐藏覆盖层。
3. error 状态：prepare 失败、start 异常和播放器错误回调会显示错误信息。
4. retry 行为：错误状态展示“重试”按钮，点击后复用最近一次 `videoPath` 和组件配置重新 `setVideoPath()` + `start()`。
5. release 安全性：播放器回调统一切回主线程，`released` 标记会忽略 release 后的旧回调。
6. 构建结果：通过，2026-06-13 执行 `./gradlew :echplayer:assembleDebug :app:assembleDebug -x test --console=plain`。

============================================================
七、回归清单
============================================================

必须回归：

1. 本地 H.264 MP4 AUTO 硬解播放。
2. 本地 H.265 MP4 AUTO 硬解或明确回退。
3. 本地 MP4 seek。
4. 硬解截图 PNG。
5. 硬解录制 MKV。
6. pause / resume / stop / release。
7. Demo 主要按钮在 Pixel 2 模拟器上可操作。

============================================================
八、已知边界
============================================================

1. RTSP 需要真实摄像头或稳定 RTSP server，不能用不可工作的临时源伪造通过。
2. HLS v1.5 只验证基础播放，不承诺加密、多码率切换和字幕。
3. HTTPS 受 FFmpeg 构建和证书链影响，失败时需要记录具体错误。
4. 网络统计以“可诊断”为目标，不作为精准带宽测速工具。
5. PlayerView v1.5 只做基础状态，不做完整全屏播放器。
