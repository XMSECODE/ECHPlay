# v2.4 验证报告

## 已实现

1. 新增 `setMuted()` 和 `isMuted()`。
2. 新增 `setSpeed()` 和 `getSpeed()`。
3. 新增 `requestAudioFocus()`、`abandonAudioFocus()`、`isAudioFocusGranted()`。
4. 新增 `OPTION_PLAYBACK_SPEED`。
5. 新增 `PROP_FLOAT_AUDIO_DELAY`。
6. 静音状态会影响 AudioTrack 左右声道输出。
7. AudioTrack 初始化时估算音频延迟，并尝试应用当前播放速度。

## 编译检查

已通过：`cd android && ./tools/smoke_check.sh`，Debug/Release AAR 和 Demo Debug APK 构建成功。

## 手工验证建议

1. 播放本地 MP4，调用 `setMuted(true/false)`，确认声音切换。
2. 调用 `setVolume(0.3f, 0.3f)` 后再静音/取消静音，确认音量恢复到原值。
3. Activity 启动播放前调用 `requestAudioFocus(this)`，停止时调用 `abandonAudioFocus(this)`。
4. 调用 `setSpeed(1.25f)`，确认 `PROP_FLOAT_PLAYBACK_RATE` 返回 1.25。
5. 播放后读取 `PROP_FLOAT_AUDIO_DELAY`，确认返回非负估算值。

## 已知边界

1. 当前速度控制主要依赖 AudioTrack playback rate，可能改变音调。
2. 暂未引入 SoundTouch 或等价音频变速不变调算法。
3. 音频焦点不会自动暂停播放，只做安全静音，避免干扰播放器主状态机。
