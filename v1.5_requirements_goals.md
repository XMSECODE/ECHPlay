# ECHPlay v1.5 需求目标分解文档

版本定位：v1.5 聚焦“网络播放稳定性和可观测性”。v1.4 已完成 MediaCodec 硬解第一版，本地 H.264 / H.265 MP4 已能在模拟器上走硬解路径。v1.5 不继续堆解码器能力，而是把播放器从“本地视频可硬解播放”推进到“网络流可诊断、可恢复、可验证”。

============================================================
一、v1.5 版本主题
============================================================

v1.5 主题：网络协议、弱网恢复和播放统计。

核心目标：

1. 系统验证 HTTP / HTTPS MP4 播放和 seek。
2. 验证 HLS / m3u8 基础播放能力。
3. 补齐 RTSP H.264 TCP / UDP 真实播放验证。
4. 增加 RTSP 断流自动重连能力。
5. 增加网速、缓冲、队列长度、解码帧率等基础统计。
6. Demo 展示网络状态、缓冲状态、重连次数和统计信息。
7. PlayerView 增加 loading / buffering / error / retry 基础状态。
8. 建立协议能力表和 v1.5 回归验证报告。

一句话目标：

v1.5 完成后，业务方可以更清楚地知道网络播放为什么卡、是否正在重连、当前网络吞吐如何，并能用固定清单验证 RTSP、HTTP、HTTPS、HLS 的基础播放能力。

============================================================
二、从 v1.4 拆出的遗留目标
============================================================

v1.4 已完成：

1. MediaCodec H.264 / H.265 本地 MP4 硬解。
2. AUTO / SOFTWARE / MEDIACODEC 解码模式。
3. 硬解失败回退软解和回退原因展示。
4. 硬解路径下截图和录制基础能力。
5. H.264 / H.265 模拟器播放验证。

v1.4 留给 v1.5 的目标：

1. RTSP H.264 TCP / UDP 真实源验证。
2. RTSP 断流后的自动重连。
3. 网络播放的缓冲百分比、网速和队列长度统计。
4. HTTP / HTTPS / HLS 协议基础验证。
5. 不支持 HEVC 的真机上 H.265 回退软解验证。
6. 长时间播放、暂停恢复、seek、停止释放的稳定性验证。
7. 协议和设备兼容性矩阵。

v1.5 选择承接的范围：

1. 承接 RTSP、HTTP、HTTPS、HLS 基础播放和错误诊断。
2. 承接 RTSP 断流重连和重连 UI 展示。
3. 承接网络统计和基础性能观察。
4. 承接真机/模拟器验证报告。

v1.5 暂不承接的范围：

1. 不做 SurfaceTexture / Surface 零拷贝硬解渲染闭环。
2. 不做完整设备黑名单 / 白名单策略，只预留数据结构和文档入口。
3. 不做 RTMP、SRT、WebRTC 等新协议。
4. 不做完整全屏播放器交互。
5. 不做 DRM、字幕、多音轨、倍速。
6. 不做完整自动化测试平台，只做可复用 smoke 验证脚本或命令清单。

============================================================
三、对比 ijkplayer 的差距和 v1.5 取舍
============================================================

ijkplayer 已有优势：

1. 网络协议验证更充分。
2. 弱网、缓冲、错误恢复经验更成熟。
3. 播放统计和调试信息更完整。
4. 长时间播放和复杂机型兼容性更充分。

ECHPlay 当前差距：

1. RTSP 只有代码路径和 UI 入口，缺少真实设备/稳定服务端实测记录。
2. HTTP / HTTPS / HLS 没有系统验证。
3. 只有基础 buffering 事件，缺少准确百分比和队列统计。
4. 没有自动重连。
5. 没有网速、首开耗时、首帧耗时、解码帧率等数据。
6. PlayerView 缺少 loading / buffering / error / retry 状态。
7. 缺少协议能力表和发版验证矩阵。

v1.5 追齐策略：

1. 先做基础协议和网络状态闭环，不追求 ijkplayer 全量协议能力。
2. 先做 RTSP 自动重连，不做复杂弱网自适应策略。
3. 先做可读统计，不做完整性能面板。
4. 先做 Demo 和 PlayerView 基础状态，不做完整业务播放器 UI。

============================================================
四、v1.5 不做什么
============================================================

以下内容不进入 v1.5 必须完成范围：

1. 不做 Surface 零拷贝硬解渲染。
2. 不做完整机型黑名单 / 白名单。
3. 不做 RTMP / SRT / WebRTC。
4. 不做 DRM。
5. 不做字幕和多音轨。
6. 不做倍速播放。
7. 不做完整全屏播放器。
8. 不做长期 24 小时稳定性实验作为必须验收项。
9. 不把所有网络异常都做成自动恢复，先覆盖断流、超时、连接拒绝、读取失败。

============================================================
五、v1.5 需求分解
============================================================

需求 1：协议能力表和测试源准备

目标：

1. 新增协议能力表，记录本地文件、RTSP、HTTP、HTTPS、HLS 的支持状态。
2. 为每种协议记录测试源、编码格式、是否支持 seek、是否支持硬解。
3. 明确哪些协议是“已实现代码路径”，哪些是“已实测通过”。
4. 提供本地验证建议，例如本机 HTTP server、HTTPS server、HLS 样例、真实 RTSP 设备。

建议文件：

1. `v1.5_requirements_goals.md`
2. `v1.5_validation_report.md`
3. `README.md`

验收标准：

1. 文档中有协议能力表。
2. 每个协议都有验证方法。
3. 未验证项目不能标记为通过。
4. README 中能看到 v1.5 协议支持说明。

优先级：P0。

============================================================

需求 2：HTTP / HTTPS MP4 基础播放验证

目标：

1. 验证 HTTP MP4 可 prepare、start、pause、resume、seek、stop。
2. 验证 HTTPS MP4 可 prepare、start、pause、resume、seek、stop。
3. 确认 HTTP / HTTPS 下 H.264 可走 AUTO 硬解或明确回退。
4. 错误 URL、连接拒绝、超时能返回明确错误码和提示。
5. Demo 支持输入 HTTP / HTTPS URL 并展示协议类型。

涉及模块：

1. `ECHPlayer`
2. `NativePlayer`
3. `MainActivity`
4. `ECHPlayerView`

验收标准：

1. HTTP MP4 能播放。
2. HTTPS MP4 能播放。
3. HTTP / HTTPS MP4 能 seek。
4. 错误 URL 不崩溃，并返回错误码。
5. Demo 日志能显示当前协议和错误原因。

优先级：P0。

============================================================

需求 3：HLS / m3u8 基础播放验证

目标：

1. 验证 HLS VOD m3u8 可播放。
2. 验证 HLS 直播 m3u8 可播放或返回明确不支持 seek。
3. Demo 能识别 `.m3u8` URL 并展示协议类型为 HLS。
4. HLS 错误地址能返回明确错误。
5. 文档记录 HLS 当前边界。

边界：

1. v1.5 不承诺所有 HLS 加密、字幕、多码率自适应。
2. v1.5 不做 HLS 码率切换 UI。
3. v1.5 只验证 FFmpeg 当前构建能力范围内的基础 HLS。

验收标准：

1. VOD m3u8 可播放。
2. 直播 m3u8 可播放或返回明确错误。
3. 不支持 seek 的直播流拖动时有清晰提示。
4. README 和验证报告记录 HLS 边界。

优先级：P0。

============================================================

需求 4：RTSP TCP / UDP 真实源验证

目标：

1. 使用真实 RTSP 摄像头或稳定 RTSP server 验证 TCP。
2. 使用同一测试源验证 UDP。
3. 验证 RTSP H.264 在 AUTO 模式下可硬解或回退软解。
4. 验证 RTSP 地址错误、鉴权失败、端口拒绝、读取超时的错误提示。
5. 记录 RTSP 测试源、编码格式、传输方式、实际解码方式。

验收标准：

1. RTSP TCP 可播放或返回明确错误。
2. RTSP UDP 可播放或返回明确错误。
3. Demo 能展示 RTSP 传输方式、实际解码方式、错误原因。
4. 验证报告不能再只写“待补测”，必须记录至少一个真实测试源结果。

优先级：P0。

============================================================

需求 5：RTSP 自动重连

目标：

1. 增加自动重连开关。
2. 增加最大重连次数。
3. 增加重连间隔。
4. 增加正在重连、重连成功、重连失败 info 回调。
5. RTSP 断流、读取超时、连接中断时按配置尝试重连。
6. stop / release 后不再重连。
7. seek 不支持的 RTSP 直播流不触发无意义 seek 恢复。

建议 API：

1. `setReconnectEnabled(boolean enabled)`
2. `setReconnectConfig(int maxRetryCount, long retryIntervalMs)`
3. `getReconnectCount()`
4. `INFO_RECONNECTING`
5. `INFO_RECONNECTED`
6. `INFO_RECONNECT_FAILED`

建议 option：

1. `OPTION_RECONNECT`
2. `OPTION_RECONNECT_MAX_COUNT`
3. `OPTION_RECONNECT_INTERVAL_MS`

验收标准：

1. RTSP 播放中断后会进入重连状态。
2. 未超过最大次数时按间隔重试。
3. 重连成功后继续播放。
4. 超过最大次数后回调错误，不死循环。
5. 用户主动 stop / release 后不会继续重连。
6. Demo 显示重连次数和最后一次重连原因。

优先级：P0。

============================================================

需求 6：网速、缓冲和队列长度统计

目标：

1. 增加当前读取速度统计。
2. 增加累计读取字节数统计。
3. 增加视频 packet 队列长度。
4. 增加音频 packet 队列长度。
5. 增加缓冲百分比或缓冲状态估算。
6. 增加统计回调和主动查询 API。
7. Demo 周期刷新统计信息。

建议 API：

1. `getReadBytes()`
2. `getNetworkSpeedBytesPerSecond()`
3. `getVideoPacketQueueSize()`
4. `getAudioPacketQueueSize()`
5. `getBufferedPercent()`
6. `getPlaybackStats()`

建议 info：

1. `INFO_STATS_UPDATE`
2. `INFO_BUFFERING_START`
3. `INFO_BUFFERING_END`

验收标准：

1. 本地文件播放统计不崩溃，网速可显示为 0 或本地读取速度。
2. HTTP / HTTPS 播放能显示读取速度。
3. RTSP 播放能显示读取速度和队列长度。
4. 队列增长或耗尽时 Demo 能观察到变化。
5. 统计读取不影响播放线程稳定性。

优先级：P0。

============================================================

需求 7：首开耗时、首帧耗时和解码帧率

目标：

1. 记录 prepare 开始时间。
2. 记录 prepare 完成时间。
3. 记录 start 调用时间。
4. 记录首帧渲染时间。
5. 统计视频解码帧数。
6. 统计近似 decode fps。
7. Demo 展示首开耗时、首帧耗时、decode fps。

验收标准：

1. 本地 MP4 能显示 prepare 耗时和首帧耗时。
2. HTTP / HTTPS 能显示 prepare 耗时和首帧耗时。
3. H.264 / H.265 硬解能显示 decode fps。
4. 统计数据重置、stop、release 后不串数据。

优先级：P1。

============================================================

需求 8：PlayerView loading / buffering / error / retry 状态

目标：

1. PlayerView 播放前显示 loading。
2. buffering start 时显示 buffering。
3. buffering end 后隐藏 buffering。
4. 播放错误时显示 error 文案。
5. error 状态提供 retry 按钮。
6. retry 使用上一次 videoPath 和配置重新播放。
7. 对外提供状态监听或简化状态文本。

验收标准：

1. PlayerView 本地播放 loading 状态正常。
2. PlayerView 网络播放 buffering 状态可见。
3. 错误 URL 显示错误，不崩溃。
4. 点击 retry 可以重新 prepare。
5. 退出页面后不会因为 retry 或回调访问已释放 player。

优先级：P1。

============================================================

需求 9：真机兼容性和回归验证

目标：

1. 至少记录一台真实 Android 设备的 H.264 MP4 硬解结果。
2. 至少记录一台支持 HEVC 设备的 H.265 硬解结果，若没有设备则明确说明。
3. 至少记录一台不支持 HEVC 或禁用硬解场景下的回退表现，若没有设备则用强制异常流验证回退。
4. 验证截图、录制、pause、resume、seek、stop、release。
5. 建立 `v1.5_validation_report.md`。

验收标准：

1. 验证报告记录设备型号、Android 版本、编码格式、协议、实际解码方式。
2. 真机无法获得的项目必须写明原因。
3. 不能把未验证项目标记为通过。
4. 发版前清单完整。

优先级：P1。

============================================================

需求 10：Demo 网络调试面板

目标：

1. 主 Demo 展示协议类型。
2. 主 Demo 展示 RTSP TCP / UDP。
3. 主 Demo 展示重连次数。
4. 主 Demo 展示网速。
5. 主 Demo 展示音视频队列长度。
6. 主 Demo 展示 prepare 耗时、首帧耗时。
7. 主 Demo 展示错误码和建议处理方式。

验收标准：

1. Demo 文案清楚，不需要看 logcat 才能知道状态。
2. 错误状态不会刷屏。
3. 页面较小设备上主要按钮仍可操作。
4. README 有 Demo 字段说明。

优先级：P1。

============================================================

需求 11：协议和弱网文档

目标：

1. README 增加 v1.5 网络播放说明。
2. README 增加 HTTP / HTTPS / HLS 示例。
3. README 增加 RTSP 重连配置示例。
4. README 增加统计 API 示例。
5. v1.5 验证报告记录测试源、测试步骤和结果。

验收标准：

1. 新人可按 README 跑 HTTP MP4。
2. 新人可按 README 配置 RTSP 重连。
3. 新人可按 README 查看播放统计。
4. 文档明确当前协议边界。

优先级：P0。

============================================================

六、任务优先级
============================================================

P0 必须完成：

1. 协议能力表和测试源准备。
2. HTTP / HTTPS MP4 基础播放验证。
3. HLS / m3u8 基础播放验证。
4. RTSP TCP / UDP 真实源验证。
5. RTSP 自动重连。
6. 网速、缓冲和队列长度统计。
7. README 和 v1.5 验证报告。

P1 应完成：

1. 首开耗时、首帧耗时和 decode fps。
2. PlayerView loading / buffering / error / retry。
3. 真机兼容性矩阵。
4. Demo 网络调试面板。

P2 可延后：

1. 4K 软硬解 CPU 对比。
2. 长时间 24 小时 RTSP 稳定性。
3. 设备黑名单 / 白名单策略落地。
4. Surface 零拷贝硬解渲染评估。
5. 自动化 smoke test 脚本完整化。

============================================================

七、建议开发步骤
============================================================

步骤 1：补协议能力表和 v1.5 验证报告骨架

目标：

1. 新增 `v1.5_validation_report.md`。
2. README 增加 v1.5 网络播放章节。
3. 明确 HTTP、HTTPS、HLS、RTSP 的测试源要求。

建议提交：

`新增v1.5网络播放规划 / add v1.5 network playback plan`

============================================================

步骤 2：验证并修正 HTTP / HTTPS 播放

目标：

1. 准备 HTTP MP4 测试源。
2. 准备 HTTPS MP4 测试源。
3. 验证 prepare / start / seek / stop。
4. 修正错误提示和协议识别。

建议提交：

`验证HTTP和HTTPS播放 / validate HTTP and HTTPS playback`

============================================================

步骤 3：验证并修正 HLS 播放

目标：

1. 准备 VOD m3u8 测试源。
2. 准备直播 m3u8 测试源或公开样例。
3. 验证播放和 seek 边界。
4. 文档记录 HLS 当前能力。

建议提交：

`验证HLS基础播放 / validate basic HLS playback`

============================================================

步骤 4：补 RTSP 真实源验证

目标：

1. 准备真实 RTSP 摄像头或稳定 RTSP server。
2. 验证 TCP。
3. 验证 UDP。
4. 验证硬解或回退。
5. 更新验证报告。

建议提交：

`补齐RTSP真实源验证 / add RTSP real-source validation`

============================================================

步骤 5：实现 RTSP 自动重连

目标：

1. Java API 和 option。
2. Native 重连状态和重试循环。
3. info 回调。
4. stop / release 取消重连。
5. Demo 展示重连次数。

建议提交：

`新增RTSP自动重连 / add RTSP auto reconnect`

============================================================

步骤 6：新增播放统计

目标：

1. read bytes。
2. network speed。
3. packet queue size。
4. buffered percent。
5. decode fps。
6. stats API 和 Demo 展示。

建议提交：

`新增网络和播放统计 / add network and playback stats`

============================================================

步骤 7：增强 PlayerView 网络状态

目标：

1. loading。
2. buffering。
3. error。
4. retry。
5. release 安全。

建议提交：

`增强PlayerView网络状态 / improve PlayerView network states`

============================================================

步骤 8：回归验证和文档收尾

目标：

1. 构建 Debug / Release。
2. 跑协议验证清单。
3. 跑本地硬解回归。
4. 更新 README。
5. 更新 `v1.5_validation_report.md`。

建议提交：

`补齐v1.5回归验证文档 / add v1.5 regression validation docs`

============================================================

八、v1.5 验收标准总表
============================================================

必须通过：

1. `./gradlew :echplayer:assembleDebug`
2. `./gradlew :echplayer:assembleRelease`
3. `./gradlew :app:assembleDebug -x test`
4. 本地 H.264 MP4 AUTO 硬解回归通过。
5. 本地 H.265 MP4 AUTO 硬解或明确回退通过。
6. HTTP MP4 可播放和 seek。
7. HTTPS MP4 可播放和 seek。
8. HLS VOD m3u8 可播放。
9. RTSP TCP 可播放或返回明确错误。
10. RTSP UDP 可播放或返回明确错误。
11. RTSP 断流后可按配置重连。
12. stop / release 后不会继续重连。
13. Demo 能显示协议类型、网速、队列长度、重连次数。
14. PlayerView 错误 URL 不崩溃，并显示 error / retry。
15. README 包含 v1.5 网络播放说明。
16. `v1.5_validation_report.md` 记录真实验证结果。

建议通过：

1. HLS 直播流可播放。
2. HLS 直播流 seek 有明确提示。
3. HTTP / HTTPS 错误地址能区分连接失败、超时、404。
4. RTSP 鉴权失败提示清晰。
5. 真机 H.264 / H.265 硬解结果已记录。
6. 1080p 软硬解 CPU 对比有初步数据。
7. 连续 pause / resume 10 次不崩溃。
8. 连续 seek 5 次不死锁。

============================================================

九、v1.5 风险点
============================================================

风险 1：缺少稳定 RTSP 测试源

规避：

1. 优先使用真实摄像头。
2. 如果使用软件 RTSP server，记录 server 名称和配置。
3. 没有测试源时不能把 RTSP 标记为通过。

风险 2：HLS 支持取决于 FFmpeg 构建能力

规避：

1. 先用简单 VOD m3u8 验证。
2. 对加密、多码率、字幕写清边界。
3. 失败时记录 FFmpeg 错误信息。

风险 3：自动重连和状态机冲突

规避：

1. stop / release 优先级最高。
2. 重连状态独立标记。
3. 重连前后清理 packet 队列和解码器。
4. 重连失败必须退出，不死循环。

风险 4：统计数据影响播放线程

规避：

1. 统计字段使用轻量原子变量或短锁。
2. UI 定时读取，不在播放线程频繁回调大对象。
3. 统计读取失败不影响播放。

风险 5：Demo 信息过多导致操作困难

规避：

1. 主要按钮保持屏幕内可操作。
2. 统计信息放到可滚动日志区域。
3. PlayerView 只展示必要状态。

============================================================

十、v1.5 完成定义
============================================================

v1.5 完成时，必须满足：

1. HTTP MP4 基础播放和 seek 已验证。
2. HTTPS MP4 基础播放和 seek 已验证。
3. HLS VOD m3u8 基础播放已验证。
4. RTSP TCP / UDP 有真实源验证记录，或明确记录外部条件阻塞原因。
5. RTSP 自动重连 API、option、info 回调完成。
6. stop / release 后不会继续重连。
7. Demo 能展示协议类型、网速、队列长度、缓冲状态、重连次数。
8. PlayerView 有 loading / buffering / error / retry 基础状态。
9. 本地 H.264 / H.265 硬解、截图、录制不因 v1.5 退化。
10. README 包含 v1.5 网络播放、重连、统计 API 示例。
11. `v1.5_validation_report.md` 记录构建、协议、真机或模拟器验证结果。

============================================================

十一、v1.5 完成后再进入的下一阶段
============================================================

v1.5 完成后，建议进入 v1.6：

1. getMediaInfo / getTrackInfo。
2. 更完整的 Debug 面板。
3. 首帧、卡顿、丢帧、音视频同步统计增强。
4. 设备硬解黑名单 / 白名单策略。
5. SurfaceTexture / Surface 零拷贝硬解渲染评估。

原因：

v1.5 解决“网络流能不能稳定播放、出问题能不能看懂”的问题。下一阶段再补媒体信息、调试面板和机型策略，播放器会更接近成熟 ijkplayer 的工程可维护性。
