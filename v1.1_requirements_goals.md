# ECHPlay v1.1 规划需求目标文档

版本定位：v1.1 不是继续堆播放器功能，而是把 v1.0 已经跑通的播放、RTSP、截图、录制能力，整理成更接近 ijkplayer 使用体验的播放器库雏形。

参考对象：

1. ijkplayer：Android/iOS video player based on FFmpeg，Android 侧提供 MediaPlayer-like API、NativeWindow/OpenGL ES 输出、AudioTrack/OpenSL ES 音频输出、MediaCodec 硬解能力。
2. 本仓库现状：v1.0 已具备本地文件播放、RTSP 播放、TCP/UDP 切换、seek 进度条、解码帧截图、播放流录制等 Demo 能力。

============================================================
一、v1.1 版本主题
============================================================

v1.1 主题：播放器 SDK 化第一步。

核心目标：

1. 让播放器从 Demo 代码走向可复用 API。
2. 建立清晰的播放器状态机和回调体系。
3. 提升 RTSP / 网络流播放的稳定性和可诊断性。
4. 让截图、录制、seek 这些 v1.0 能力在生命周期上更安全。
5. 保留后续接入 OpenGL、MediaCodec、独立 library module 的接口空间。

一句话标准：

其他页面或项目可以像使用一个播放器组件一样使用 ECHPlayer，而不是只能改 MainActivity 来验证功能。

============================================================
二、v1.1 不做什么
============================================================

以下内容不进入 v1.1 必须完成范围：

1. 不做完整复刻 ijkplayer。
2. 不做 MediaCodec 硬解正式接入。
3. 不做 OpenGL YUV 三纹理正式渲染。
4. 不做 HLS / m3u8 完整支持承诺。
5. 不做 RTMP。
6. 不做播放器皮肤体系。
7. 不做 Maven 发布。

这些能力可以预留接口或写入后续路线，但 v1.1 的重点是 API、状态、回调、网络稳定性和 Demo 可验证。

============================================================
三、v1.1 需求与达成目标
============================================================

需求 1：播放器 API 向 ijkplayer / MediaPlayer 风格收敛

目标：

1. 将当前 ECHPlayer API 整理成更标准的播放器生命周期。
2. 支持 prepareAsync、start、pause、stop、reset、release。
3. 保留 setDataSource、setSurface、seekTo、getDuration、getCurrentPosition。
4. 增加 isPlaying、isPrepared、isReleased 等状态查询。
5. 增加 setOption(category, name, value) 风格接口，为 RTSP、timeout、buffer、debug 等参数预留通道。

验收标准：

1. MainActivity 不直接依赖 native 细节。
2. 一个新 Activity 只通过 ECHPlayer API 就能播放本地文件和 RTSP。
3. 重复 start / pause / resume / stop / release 不崩溃。
4. 非法状态调用能返回明确错误或回调错误，不产生 native 崩溃。

============================================================

需求 2：建立播放器状态机

目标：

1. 增加明确状态：IDLE、INITIALIZED、PREPARING、PREPARED、STARTED、PAUSED、STOPPED、COMPLETED、ERROR、RELEASED。
2. Java 层和 native 层状态保持一致。
3. 所有公开方法先检查状态，再执行动作。
4. Surface 销毁、Activity 退出、播放完成、网络失败都能走到确定状态。

验收标准：

1. release 后再次调用播放控制 API 不崩溃。
2. Surface destroyed 时能停止渲染线程，不访问失效 ANativeWindow。
3. 播放完成后状态进入 COMPLETED。
4. 出错后状态进入 ERROR，并可 reset 后重新播放。

============================================================

需求 3：完善回调体系和错误码

目标：

1. 增加 OnPreparedListener。
2. 增加 OnCompletionListener。
3. 增加 OnErrorListener。
4. 增加 OnInfoListener。
5. 增加 OnBufferingUpdateListener。
6. 定义错误码和 info 码，不再只返回字符串日志。

建议错误码：

1. ERROR_OPEN_INPUT_FAILED
2. ERROR_STREAM_INFO_FAILED
3. ERROR_NO_VIDEO_STREAM
4. ERROR_DECODER_OPEN_FAILED
5. ERROR_NETWORK_TIMEOUT
6. ERROR_RTSP_AUTH_FAILED
7. ERROR_RENDER_SURFACE_INVALID
8. ERROR_RECORD_FAILED

建议 info 码：

1. INFO_PREPARE_STARTED
2. INFO_VIDEO_RENDERING_START
3. INFO_AUDIO_RENDERING_START
4. INFO_BUFFERING_START
5. INFO_BUFFERING_END
6. INFO_SEEK_COMPLETE
7. INFO_RECORDING_START
8. INFO_RECORDING_END

验收标准：

1. UI 不再靠解析字符串判断播放结果。
2. RTSP 连接失败能通过错误码区分超时、鉴权失败、无视频流。
3. seek 完成、首帧渲染、录制开始和结束都有回调。
4. Demo 页面能把错误码和中文错误信息展示出来。

============================================================

需求 4：RTSP / 网络播放稳定性增强

目标：

1. 保留 RTSP TCP / UDP 切换。
2. 将 timeout、rw_timeout、buffer_size、max_delay 改为可配置 option。
3. 增加网络读取超时后的错误回调。
4. 增加基础缓冲状态：队列不足时 BUFFERING_START，恢复后 BUFFERING_END。
5. 增加网络流不可 seek 的判断，避免直播流拖动导致状态混乱。

验收标准：

1. RTSP 地址错误时 5 秒左右返回错误，不无限卡住。
2. RTSP 账号密码错误时能给出鉴权失败提示。
3. 网络中断时能进入 ERROR 或 BUFFERING，不让 UI 误以为还在正常播放。
4. 直播流进度条不显示可拖动 seek，或拖动时给出明确提示。

============================================================

需求 5：seek 稳定性和音视频同步恢复

目标：

1. seek 时统一进入 SEEKING 状态。
2. seek 前停止或暂停 demux / decode 取包。
3. seek 后清空 packet queue。
4. seek 后 flush audio/video decoder。
5. seek 后重置 audio clock，并等待新音频时钟建立。
6. seek 完成后触发 INFO_SEEK_COMPLETE。

验收标准：

1. 本地 MP4 连续快速拖动 10 次不死锁。
2. seek 后音视频能恢复同步。
3. seek 到接近结尾时能正常 completed。
4. 不支持 seek 的流不会触发 native seek 崩溃。

============================================================

需求 6：截图和录制能力产品化

目标：

1. 截图继续保存解码后原始帧 PNG，不保存 SurfaceView 屏幕画面。
2. 截图接口返回文件路径、宽、高、时间戳。
3. 录制接口明确状态：未录制、录制中、停止中、录制失败。
4. 录制失败通过错误码回调。
5. stop、release、Surface 销毁时自动安全停止录制。
6. Demo 页面显示最近截图和录制文件路径。

验收标准：

1. 播放中截图连续点击 5 次不崩溃。
2. 开始录制后停止播放，录制文件能正确写尾。
3. seek 过程中录制状态明确，不生成损坏文件或静默失败。
4. 录制文件路径可在日志中直接看到。

============================================================

需求 7：新增 PlayerView 雏形

目标：

1. 新增 ECHPlayerView，内部管理 SurfaceView。
2. ECHPlayerView 提供 setVideoPath、start、pause、stop、release。
3. 封装 SurfaceHolder.Callback，减少 Activity 直接处理 Surface 生命周期。
4. 提供基础控制条：播放、暂停、停止、截图、录制。
5. 控制条不追求美化，优先验证组件边界。

验收标准：

1. MainActivity 可以从大量播放器控制代码中瘦身。
2. 新增一个最小 Demo 页面，只使用 ECHPlayerView 即可播放。
3. 旋转屏幕或退出页面时 release 不崩溃。

============================================================

需求 8：Demo 和文档补齐

目标：

1. README 增加 v1.1 使用示例。
2. Demo 页面保留本地文件和 RTSP 两种入口。
3. Demo 页面展示状态、错误码、info 回调、截图路径、录制路径。
4. 增加简单测试清单，覆盖本地播放、RTSP 播放、截图、录制、seek、release。

验收标准：

1. 新人按 README 可以跑起本地 MP4。
2. 新人按 README 可以填 RTSP URL 测试。
3. v1.1 发版前能按测试清单逐项验证。

============================================================

四、v1.1 优先级
============================================================

P0 必须完成：

1. 标准播放器 API。
2. 播放器状态机。
3. 回调体系和错误码。
4. RTSP 超时和错误回调。
5. release / stop / Surface 销毁稳定性。

P1 应该完成：

1. seek 稳定性增强。
2. 截图和录制状态产品化。
3. PlayerView 雏形。
4. Demo 页面瘦身。

P2 可以顺延：

1. 缓冲百分比精确计算。
2. 网速统计。
3. 自动重连。
4. HTTP / HTTPS / HLS 的完整兼容测试。
5. 独立 Android library module。

============================================================

五、v1.1 完成定义
============================================================

v1.1 完成时，必须满足：

1. app 能编译通过：./gradlew :app:assembleDebug -x test。
2. 本地 test.mp4 能播放、暂停、恢复、seek、截图、录制。
3. RTSP URL 能播放，并支持 TCP / UDP 切换。
4. RTSP 错误能通过错误码和中文信息回调到 UI。
5. 退出页面、Surface 销毁、release 不崩溃。
6. Demo 能展示状态、错误、info、截图路径、录制路径。
7. README 或独立文档说明 v1.1 API 使用方式。

============================================================

六、v1.1 后续版本衔接
============================================================

v1.2 建议方向：

1. 独立 Android library module。
2. OpenGL YUV 渲染。
3. MediaCodec 硬解和软解回退。
4. HTTP / HTTPS / HLS 完整网络播放。
5. 更完整的 PlayerView 控制条和全屏。
6. 多 ABI 支持和 FFmpeg 裁剪。

v1.1 的价值是把当前播放器从“功能 Demo”变成“可继续扩展的播放器组件”。只要状态机、回调、错误码、生命周期稳定下来，后续接 OpenGL、硬解、HLS 都会轻很多。
