# v2.4 音频系统对齐目标

## 版本定位

v2.4 对齐 ijkplayer 常用音频控制入口，重点补齐音量、静音、音频焦点、播放速度入口和音频延迟统计，让业务侧可以统一管理音频行为。

## 目标拆解

1. 音量和静音

   - 保留 `setVolume(left, right)`。
   - 新增 `setMuted(boolean)`。
   - 新增 `isMuted()`。
   - 静音状态即时应用到 `AudioTrack`。

2. 音频焦点

   - 新增 `requestAudioFocus(Context)`。
   - 新增 `abandonAudioFocus(Context)`。
   - 新增 `isAudioFocusGranted()`。
   - 音频焦点丢失时自动静音，重新获得焦点时恢复声音。

3. 播放速度

   - 新增 `setSpeed(float)` 和 `getSpeed()`。
   - 新增 `playback_speed` option。
   - `PROP_FLOAT_PLAYBACK_RATE` 返回当前期望速度。
   - 当前阶段优先应用到 `AudioTrack.setPlaybackRate()`，后续继续接入 native 音视频时钟。

4. 音频统计

   - 新增 `PROP_FLOAT_AUDIO_DELAY`。
   - 根据 AudioTrack buffer size 估算当前音频输出延迟。

## 达成标准

1. 新增函数、字段和常量都有中文注释。
2. 新增 API 写入 `IECHMediaPlayer`。
3. 静音和音量共用同一条 AudioTrack 应用路径。
4. 编译 smoke check 通过。

## 后续边界

SoundTouch 或等价变调/不变调速度处理，需要深入 native 音频重采样和音视频同步，本版本先提供稳定入口和基础 AudioTrack 变速能力。
